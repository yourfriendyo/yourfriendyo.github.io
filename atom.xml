<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://yourfriendyo.github.io</id>
    <title>AKA你的闺蜜的博客</title>
    <updated>2023-02-12T02:43:55.196Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://yourfriendyo.github.io"/>
    <link rel="self" href="https://yourfriendyo.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://yourfriendyo.github.io/images/avatar.png</logo>
    <icon>https://yourfriendyo.github.io/favicon.ico</icon>
    <rights>All rights reserved 2023, AKA你的闺蜜的博客</rights>
    <entry>
        <title type="html"><![CDATA[C语言深度解剖]]></title>
        <id>https://yourfriendyo.github.io/post/c-yu-yan-shen-du-jie-pou/</id>
        <link href="https://yourfriendyo.github.io/post/c-yu-yan-shen-du-jie-pou/">
        </link>
        <updated>2023-02-02T10:10:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-关键字">1. 关键字</h2>
<p>标准C语言，c89 中共有32个关键字，c99 又新增了5个。</p>
<h3 id="11-auto">1.1 auto</h3>
<p>auto 一般用来修饰局部变量，被 auto 修饰的局部变量可以自动开辟自动释放，但局部变量本身就是自动开辟和释放的，所以 auto 没什么用。</p>
<p>auto 只能修饰局部变量，不能修饰全局变量。</p>
<h3 id="12-register">1.2 register</h3>
<p>建议编译器将 register 修饰的变量放到寄存器中。一般高频被读取的、很少被写入的、局部的变量可以被放到寄存器中。</p>
<p>因为放在寄存器，不是在内存中，所以无法取地址。</p>
<h3 id="13-extern">1.3 extern</h3>
<p>定义与声明的区别？</p>
<p>定义变量的本质就是开辟一块空间，以供变量使用。声明是告知编译器存在这样的一个变量。</p>
<p>定义只有一次，而声明可以有多次。</p>
<pre><code class="language-c">//test.c
int g_val = 1;
//main.c
extern int g_val;     // 正确
extern int g_val = 1; // 错误
</code></pre>
<ul>
<li>extern 声明变量时不能加上赋值或初始化操作。编译器认为是在定义变量，就发生变量重定义。</li>
<li>函数可以不 extern 声明，变量一定要声明。</li>
</ul>
<h3 id="14-static">1.4 static</h3>
<p>全局变量和函数都是可以跨文件访问的。</p>
<ul>
<li>static 修饰全局变量或函数，则该变量或函数只能在本文件内被访问，不能跨文件访问。</li>
<li>static 限制全局变量或函数只能在本文件中访问，限制了其的作用域，也变相的提供安全保证。</li>
</ul>
<p>static 修饰局部变量，是另一种作用，不要和上面混淆。</p>
<ul>
<li>static 修饰的局部变量只在入作用域时被定义和初始化一次，出作用域不会被销毁。</li>
<li>但出作用域不能被访问，作用域不变，只是生命周期变成全局。</li>
<li>变量从栈中移到了全局数据区。</li>
</ul>
<h3 id="15-sizeof-及类型关键字">1.5 sizeof 及类型关键字</h3>
<p>sizeof 是关键字而不是函数，<code>()</code>只是习惯用法。</p>
<p>类型的意义在于对内存使用进行合理的划分，使用场景决定了使用变量的类型，因此存在多种类型。</p>
<h3 id="16-signedunsigned">1.6 signed/unsigned</h3>
<p>有符号数和无符号数都是整数，浮点数没有这样的概念。</p>
<ul>
<li>有符号数取二进制序列的最高位为符号位，其他位为数据位。符号为为0表示整数，为1表示负数。</li>
<li>无符号数的二进制序列所有位都是数据位。</li>
</ul>
<h4 id="原反补">原反补</h4>
<p>整数的有三种二进制序列，分别是原码、反码、补码。正数的原反补码相同，负数的原反补码有如下运算逻辑：</p>
<p class='katex-block'><span class="katex-display"><span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi mathvariant="normal">原</mi><mi mathvariant="normal">码</mi><mo>→</mo><mi mathvariant="normal">符</mi><mi mathvariant="normal">号</mi><mi mathvariant="normal">位</mi><mi mathvariant="normal">不</mi><mi mathvariant="normal">变</mi><mi mathvariant="normal">，</mi><mi mathvariant="normal">其</mi><mi mathvariant="normal">他</mi><mi mathvariant="normal">位</mi><mi mathvariant="normal">安</mi><mi mathvariant="normal">位</mi><mi mathvariant="normal">取</mi><mi mathvariant="normal">反</mi><mo>→</mo><mi mathvariant="normal">反</mi><mi mathvariant="normal">码</mi><mspace linebreak="newline"></mspace><mi mathvariant="normal">反</mi><mi mathvariant="normal">码</mi><mo>→</mo><mtext>  </mtext><mi mathvariant="normal">加</mi><mi mathvariant="normal">一</mi><mspace width="1em"/><mi mathvariant="normal">（</mi><mi mathvariant="normal">符</mi><mi mathvariant="normal">号</mi><mi mathvariant="normal">位</mi><mi mathvariant="normal">参</mi><mi mathvariant="normal">与</mi><mi mathvariant="normal">运</mi><mi mathvariant="normal">算</mi><mi mathvariant="normal">）</mi><mo>→</mo><mtext>  </mtext><mi mathvariant="normal">补</mi><mi mathvariant="normal">码</mi><mspace linebreak="newline"></mspace></mrow><annotation encoding="application/x-tex">原码 \rightarrow 符号位不变，其他位安位取反 \rightarrow 反码 \\
反码 \rightarrow\; 加一 \quad（符号位参与运算） \rightarrow \; 补码 \\
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mord cjk_fallback">原</span><span class="mord cjk_fallback">码</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">→</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mord cjk_fallback">符</span><span class="mord cjk_fallback">号</span><span class="mord cjk_fallback">位</span><span class="mord cjk_fallback">不</span><span class="mord cjk_fallback">变</span><span class="mord cjk_fallback">，</span><span class="mord cjk_fallback">其</span><span class="mord cjk_fallback">他</span><span class="mord cjk_fallback">位</span><span class="mord cjk_fallback">安</span><span class="mord cjk_fallback">位</span><span class="mord cjk_fallback">取</span><span class="mord cjk_fallback">反</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">→</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0em;vertical-align:0em;"></span><span class="mord cjk_fallback">反</span><span class="mord cjk_fallback">码</span></span><span class="mspace newline"></span><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mord cjk_fallback">反</span><span class="mord cjk_fallback">码</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">→</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mord cjk_fallback">加</span><span class="mord cjk_fallback">一</span><span class="mspace" style="margin-right:1em;"></span><span class="mord cjk_fallback">（</span><span class="mord cjk_fallback">符</span><span class="mord cjk_fallback">号</span><span class="mord cjk_fallback">位</span><span class="mord cjk_fallback">参</span><span class="mord cjk_fallback">与</span><span class="mord cjk_fallback">运</span><span class="mord cjk_fallback">算</span><span class="mord cjk_fallback">）</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">→</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0em;vertical-align:0em;"></span><span class="mord cjk_fallback">补</span><span class="mord cjk_fallback">码</span></span><span class="mspace newline"></span></span></span></span></p>
<p>原码转补码是取反加一，补码转原码可以是减一取反、也可以是取反加一。</p>
<h4 id="数据的存取">数据的存取</h4>
<pre><code class="language-c">unsigned int ui = -10;
</code></pre>
<p>数据<code>-10</code>的类型是<code>signed int</code>，变量赋值就是将内存中的数据拷贝到变量的内存空间，对于整数来说，内存中存储的是补码。</p>
<p>因此，上述赋值操作就是将<code>-10</code>的补码覆盖到变量<code>ui</code>所在的4字节空间中，这4字节空间中存储的二进制序列是：</p>
<pre><code>11111111 11111111 11111111 11110110
</code></pre>
<p>又因为<code>ui</code>的类型是<code>unsigned int</code>，故<code>ui</code>默认认为这4字节数据是无符号数，故<code>ui</code>就是一个非常大的数字。</p>
<h4 id="大小端">大小端</h4>
<ul>
<li>数据按字节为单位，高权值放在高地址处，低权值放在低地址处，就是小端存储。</li>
<li>低权值放在高地址处，高权值放在低地址处，就是大端存储。</li>
</ul>
<h4 id="有符号数的最小值">有符号数的最小值</h4>
<p>signed char 类型的最小值是 –128，对应的二进制序列是 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1000</mn><mtext>  </mtext><mn>0000</mn></mrow><annotation encoding="application/x-tex">1000\;0000</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span></span></span></span> 。</p>
<pre><code>  1000 0000 原码
  1111 1111 反码
1 1000 0000 补码
</code></pre>
<p>–128 的补码存入 signed char 8bits 的空间会发生截断，可见 –128 的补码也是 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1000</mn><mtext>  </mtext><mn>0000</mn></mrow><annotation encoding="application/x-tex">1000\;0000</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span></span></span></span>。</p>
<pre><code>  1000 0000 补码
  0111 1111 反码
  0000 0000 原码
</code></pre>
<p>由于发生过截断，取出的时候，补码 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1000</mn><mtext>  </mtext><mn>0000</mn></mrow><annotation encoding="application/x-tex">1000\;0000</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span><span class="mord">0</span></span></span></span> 无法再正确转化回原码。</p>
<p>所以，类似 –128 这样，有符号数的最小值的原反补转化，是“半计算半规定”的过程。</p>
<p>故计算机规定，有符号数的补码，符号位为1、数据位为0，就认为其是有符号数的最小值。</p>
<h4 id="有符号数的范围">有符号数的范围</h4>
<img src="https://img.gejiba.com/images/2fa4db682c3c597068a022969c599de1.png" style="zoom:50%;"  >
<h3 id="17-_bool">1.7 _Bool</h3>
<p>c99 之前没有布尔类型，c99 新增了一个关键字 _Bool ，被宏定义成一个宏 bool 。类型的大小是 <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mn>1</mn></mrow><annotation encoding="application/x-tex">1</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.64444em;vertical-align:0em;"></span><span class="mord">1</span></span></span></span> 字节。</p>
<p>浮点数本身存在精度丢失，所以不可以用<code>== !=</code>来比较相等，只能判断将两者相减的结果是否小于一定误差。</p>
<pre><code class="language-cpp">fabs(x - y) &lt; DBL_EPSILON; // double 精度误差
fabs(x - y) &lt; FLT_EPSILON; // float  精度误差	
</code></pre>
<h3 id="18-continue">1.8 continue</h3>
<p>break是直接跳出循环，没有异议。</p>
<ul>
<li>对于 while 循环、do while 循环， continue 是跳到下一次循环的条件判断部分。</li>
<li>对于 for 循环，continue 是跳到本次循环的条件更新处，再进行下一次循环。</li>
</ul>
<h3 id="19-void">1.9 void</h3>
<p>C语言的函数可以不声明返回类型和参数类型，默认的返回类型参数类型是 int。C++ 不允许使用默认 int。</p>
<p>使用 void 修饰返回类型和参数类型，可以避免歧义。</p>
<p>void 更像是一种提示符，没有太大意义。</p>
<pre><code class="language-cpp">     test0()     {}
void test1()     {}
void test2(void) {}

int main() {
    int a = test(1, 2, 3, 4);
    test1(1, 2, 3, 4);
    test2(1, 2, 3, 4);
}
</code></pre>
<p>void最多是作 void* 类型指针，用来接受任意类型的指针或者赋值给任意类型的指针。</p>
<h3 id="110-return">1.10 return</h3>
<p>return 返回变量，是通过寄存器拷贝变量的值，调用方接受返回值，是在用另一个变量接受寄存器的值。寄存器中的值是不可更改的，具有常属性。</p>
<h3 id="111-const">1.11 const</h3>
<p>const 修饰的变量被称为常变量，作用是提醒编译器不能直接修改该变量，但仍可通过指针的方式间接修改。</p>
<p>const void* 是修饰指针所指向的变量不可修改，void* const 修饰的是指针本身不可修改。</p>
<p>const 修饰变量作右值的时候，要注意权限放大的问题。</p>
<h3 id="112-volatile">1.12 volatile</h3>
<p>一般如果一个变量在当前执行流中不会被修改，CPU会将其优化放到寄存器中，不会到内存中取值。</p>
<p>但可能会存在其他执行流修改该变量，如果此时CPU仍不去内存中取值，就会导致程序错误。因此在并发环境下，为避免CPU的这种错误优化，可以对变量加 volatile 修饰。</p>
<p>volatile 就是避免编译器优化，防止内存被覆盖，达到稳定访问内存到目的。</p>
<pre><code class="language-cpp">while (flg)
11d8:   8b 05 32 2e 00 00       mov    0x2e32(%rip),%eax        # 4010 &lt;flg&gt;
11de:   85 c0                   test   %eax,%eax
11e0:   75 f6                   jne    11d8 &lt;main+0x38&gt;
{}
</code></pre>
<h3 id="113-union">1.13 union</h3>
<p>vs下c语言不支持定义空结构体，gcc允许空结构体且大小为0。</p>
<p>union 内每个成员都从首字节开始存放，相当于每个成员都是第一个成员。union 的大小由最大成员决定，但也要考虑内存对齐。</p>
<h3 id="114-typedef">1.14 typedef</h3>
<ul>
<li><code>int*</code>连续定义时，只有第一个变量是int*类型，其他都是int类型。</li>
<li>如果使用 typedef 重定义的 int*，则连续定义的变量都是int*类型。</li>
</ul>
<p>typedef 的类型，算作一个全新类型，是一个整体。而 #define 是文本替换，编译时和第一种无异。</p>
<pre><code class="language-c">typedef int* intp;
#define INTP int*
int* a, b; // int*, int
intp a, b; // int*, int*
INTP a, b; // int*, int
</code></pre>
<p>C语言的五个存储类型关键字 typedef volatile auto register static，在定义变量时最多只能使用一个。</p>
<p> </p>
<h2 id="2-符号">2. 符号</h2>
<h3 id="21-char类型大小">2.1 char类型大小</h3>
<pre><code class="language-cpp">char c = '1';
printf(&quot;%c\n&quot;, c);   //1
printf(&quot;%c\n&quot;, '1'); //4
</code></pre>
<p>C99标准规定：<code>'1'</code>这样的字符常量，叫做整型字符常量，实际上是4字节的整型值。截断放入字符型变量<code>c</code>中，所以<code>c</code>占1字节。</p>
<blockquote>
<p>上面是C语言对字符常量的处理方式，C++已经遗弃了这一反直觉的设计。</p>
</blockquote>
<p>因此，<code>'123'</code>、<code>'1234'</code>这样的字符常量也是可以编译成功的，小于等于4字节就行。但我们非常不推荐这样。</p>
<h3 id="22-溢出和截断">2.2 溢出和截断</h3>
<p>如果变量运算结果超出了变量本身的长度，我们称之为溢出。</p>
<p>变量进行运算，是需要从内存中取出数据放到CPU寄存器中进行运算的。32位机器的寄存器长度为32bits。所以比较小的整型数据放到寄存器中都会发生整形提升到32bits。</p>
<p>如果运算发生溢出，就会将运算结果截取变量大小个长度，放回变量的内存区域，我们称之为截断。</p>
<h3 id="23-左移和右移">2.3 左移和右移</h3>
<ul>
<li>左移：低位补零，高位丢弃。</li>
<li>右移：
<ul>
<li>如果是无符号数，低位丢弃，高位补零；</li>
<li>如果是有符号数，低位丢弃，高位补符号位。</li>
</ul>
</li>
</ul>
<p>任何位运算操作的都是内存中的补码，只有涉及存取的时候才会考虑原反补的问题。</p>
<p>左移右移负数位的情况的结果是不可预测的，不推荐这样使用。</p>
<h3 id="24-四种小数取整方案">2.4 四种小数取整方案</h3>
<ul>
<li>浮点数到整数的隐式类型转换，就是零向取整。函数<code>trunc</code>也是零向取整。</li>
<li>函数<code>floor</code>是负向取整，也就是往小的方向取，也称地板取整。</li>
<li>函数<code>ceil</code>是正向取整，也就是往大的方向取。</li>
<li>函数<code>round</code>是四舍五入取整。</li>
</ul>
<pre><code class="language-cpp">#include &lt;math.h&gt;
double trunc(double _X);
double floor(double _X);
double ceil (double _X);
double round(double _X);
</code></pre>
<h3 id="25-负数取模">2.5 负数取模</h3>
<p class='katex-block'><span class="katex-display"><span class="katex"><span class="katex-mathml"><math><semantics><mrow><mi mathvariant="normal">满</mi><mi mathvariant="normal">足</mi><mspace width="1em"/><mi>a</mi><mo>=</mo><mi>q</mi><mo>∗</mo><mi>d</mi><mo>+</mo><mi>r</mi><mspace width="1em"/><mi mathvariant="normal">且</mi><mspace width="1em"/><mo>(</mo><mn>0</mn><mo>≤</mo><mi mathvariant="normal">∣</mi><mi>r</mi><mi mathvariant="normal">∣</mi><mo>&lt;</mo><mi mathvariant="normal">∣</mi><mi>d</mi><mi mathvariant="normal">∣</mi><mo>)</mo><mspace width="1em"/><mi mathvariant="normal">则</mi><mspace width="1em"/><mi>a</mi><mi mathvariant="normal">/</mi><mi>d</mi><mo>=</mo><mi>q</mi><mi mathvariant="normal">，</mi><mi>a</mi><mi mathvariant="normal">%</mi><mi>d</mi><mo>=</mo><mi>r</mi></mrow><annotation encoding="application/x-tex">满足\quad a=q*d+r\quad 且\quad(0≤|r|&lt;|d|)\quad则\quad a/d=q，a\%d=r
</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.43056em;vertical-align:0em;"></span><span class="mord cjk_fallback">满</span><span class="mord cjk_fallback">足</span><span class="mspace" style="margin-right:1em;"></span><span class="mord mathdefault">a</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.6597200000000001em;vertical-align:-0.19444em;"></span><span class="mord mathdefault" style="margin-right:0.03588em;">q</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">∗</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:0.77777em;vertical-align:-0.08333em;"></span><span class="mord mathdefault">d</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span><span class="mbin">+</span><span class="mspace" style="margin-right:0.2222222222222222em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord mathdefault" style="margin-right:0.02778em;">r</span><span class="mspace" style="margin-right:1em;"></span><span class="mord cjk_fallback">且</span><span class="mspace" style="margin-right:1em;"></span><span class="mopen">(</span><span class="mord">0</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">≤</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord">∣</span><span class="mord mathdefault" style="margin-right:0.02778em;">r</span><span class="mord">∣</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">&lt;</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:1em;vertical-align:-0.25em;"></span><span class="mord">∣</span><span class="mord mathdefault">d</span><span class="mord">∣</span><span class="mclose">)</span><span class="mspace" style="margin-right:1em;"></span><span class="mord cjk_fallback">则</span><span class="mspace" style="margin-right:1em;"></span><span class="mord mathdefault">a</span><span class="mord">/</span><span class="mord mathdefault">d</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.94444em;vertical-align:-0.19444em;"></span><span class="mord mathdefault" style="margin-right:0.03588em;">q</span><span class="mord cjk_fallback">，</span><span class="mord mathdefault">a</span><span class="mord">%</span><span class="mord mathdefault">d</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2777777777777778em;"></span></span><span class="base"><span class="strut" style="height:0.43056em;vertical-align:0em;"></span><span class="mord mathdefault" style="margin-right:0.02778em;">r</span></span></span></span></span></p>
<h4 id="正负余数">正负余数</h4>
<p>不同语言对负数取模运算可能不同，c语言中–10/3=–3,–10%3=–1，而python中–10/3=–4, –10%3=2。我们将两种余数分别称为正余数和负余数。</p>
<p>取模结果不同，根本原因是语言采用的整数除法取整方案不同。c语言采用的是零向取整，python采用的是负向取整。</p>
<h4 id="取余和取模">取余和取模</h4>
<p>按照定义，取余和取模并不严格相同，取余是让商零向取整，取模是让商负向取整。区别只有在取模运算的两个操作数不同符号时才体现出来。</p>
<blockquote>
<p>c语言是取余，python是取模。</p>
</blockquote>
<h4 id="计算方法">计算方法</h4>
<p>我们只要记住自身语言的整数除法是零向取整还是负向取整，然后算出整数除法的结果，再代入公式计算余数即可。</p>
<h2 id="3-预处理">3. 预处理</h2>
<p>宏在任何地方都可以定义，宏是全局的，定义后都可以使用。</p>
<p><code>#</code>可以将之后的符号，变成一个字符串。<code>##</code>可以将其左右两侧的符号拼接成一个新的符号文本。</p>
<h2 id="4指针和数组">4.指针和数组</h2>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[现代c++2023]]></title>
        <id>https://yourfriendyo.github.io/post/xian-dai-cpp-2023/</id>
        <link href="https://yourfriendyo.github.io/post/xian-dai-cpp-2023/">
        </link>
        <updated>2023-02-02T10:04:12.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-缺省参数">1. 缺省参数</h2>
<h3 id="第一题">第一题</h3>
<p>具有缺省参数的形参必须放在参数列表的右侧，这个描述并不严谨。</p>
<pre><code class="language-cpp">void f(int,       int,       int c = 10);
void f(int,       int b = 5, int);
void f(int a = 1, int,       int);

void f(int a, int b, int c) {
  std::cout &lt;&lt; a &lt;&lt; &quot; &quot; &lt;&lt; b &lt;&lt; &quot; &quot; &lt;&lt; c &lt;&lt; std::endl;
}
</code></pre>
<blockquote>
<p>上述代码是没错的。一个函数可以有多个声明。</p>
</blockquote>
<ul>
<li>在函数声明中，</li>
<li>在第一个具有缺省参数的形参之后的形参，必须也拥有缺省参数。</li>
<li>这些形参的缺省参数，可以分别放在之前的、同一作用域内的若干条函数声明中。</li>
</ul>
<blockquote>
<p>可以认为编译器会讲多条函数声明作整合。</p>
</blockquote>
<p>当然，函数声明可以相同，但不可以重复定义缺省参数。</p>
<pre><code class="language-cpp">// 正确
void f(int, int, int c = 1);
void f(int, int, int c);
// 正确
void f(int, int, int);
void f(int, int, int);

// 错误
void f(int, int, int c = 1);
void f(int, int, int c = 1);
// 错误
void f(int, int, int c = 1);
void f(int, int, int c = 2);
</code></pre>
<h3 id="第二题">第二题</h3>
<pre><code class="language-cpp">class A 
{
    A(int i);
    void func1(int i = 1);
    void func2(int i, int j = 1);
};

A::A(int i = 1) // 错误，非默认构造函数变成默认构造函数
{}
void A::func1(int i = 2) // 错误，缺省参数重定义
{}
void A::func2(int i = 1, int j) // 正确，允许在函数定义处定义缺省参数
{}
</code></pre>
<p>C++允许，在成员函数定义处给形参设置缺省参数，也会将其和类内函数声明中所提供的缺省参数进行组合。</p>
<blockquote>
<p>尽管我们并不推荐将缺省参数放在函数定义处。</p>
</blockquote>
<ul>
<li>
<p>对于非模版类的成员函数，</p>
</li>
<li>
<p>给非默认的构造函数、拷贝构造、赋值重载的形参设置缺省参数，</p>
<p>从而导致其变成默认的构造函数、拷贝构造、赋值重载。</p>
</li>
<li>
<p>是不被允许的。</p>
</li>
</ul>
<p>C++规定，模版类的成员函数的缺省参数必须放在函数声明处。</p>
<h3 id="第三题">第三题</h3>
<pre><code class="language-cpp">struct Base {
  	virtual void f(int a = 1) { std::cout &lt;&lt; &quot;Base: &quot; &lt;&lt; a &lt;&lt; std::endl; }
};
struct Derived : Base {
    void f(int a = 7) override { std::cout &lt;&lt; &quot;Dervied: &quot; &lt;&lt; a &lt;&lt; std::endl; }
};
int main() {
    Base* ptr = new Derived;
    ptr-&gt;f(); // Derived 1
}
</code></pre>
<p>子类中虚函数的覆盖函数，不会从父类中获取缺省参数。但多态情况下，调用虚函数时，<strong>虚函数的缺省参数会根据调用对象的静态类型而定</strong>。</p>
<p>编译时确定的类型就是变量的静态类型，运行时的类型就是变量的动态类型。</p>
<blockquote>
<p>所以多态调用子类函数，但缺省参数用的却是父类的。</p>
</blockquote>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[HTTP服务器]]></title>
        <id>https://yourfriendyo.github.io/post/http-fu-wu-qi/</id>
        <link href="https://yourfriendyo.github.io/post/http-fu-wu-qi/">
        </link>
        <updated>2023-01-14T18:21:46.000Z</updated>
        <content type="html"><![CDATA[<h1 id="http服务器">HTTP服务器</h1>
<h2 id="1-项目背景和技术特点">1. 项目背景和技术特点</h2>
<h3 id="实现目的">实现目的</h3>
<blockquote>
<p>从移动端到浏览器，HTTP 协议无疑是打开互联网应用窗口的重要协议，其在网络应用层中的地位不可撼动，是能准确区分前后台的重要协议。</p>
</blockquote>
<p>完善对HTTP协议的理论学习，从零开始完成WEB服务器开发。</p>
<p>采用 CS 模型，编写支持中小型应用的HTTP服务器，井结合MySQL。</p>
<p>该项目可以帮助我们从技术上理解上网输入网站URL到关闭浏览器到所有操作中的技术细节。</p>
<p><a href="https://blog.csdn.net/yourfriendyo/article/details/126188159">HTTP基础知识，这里就不再赘述了</a></p>
<h3 id="技术特点">技术特点</h3>
<ul>
<li>Linux 网络编程 TCP/IP协议、socket流式套接、http协议。</li>
<li>多线程技术</li>
<li>cgi 技术</li>
<li>shell 脚本</li>
<li>线程池</li>
</ul>
<h3 id="开发环境">开发环境</h3>
<ul>
<li>centos7</li>
<li>vim gcc/g++ Makefile</li>
</ul>
<p> </p>
<h2 id="2-代码结构和实现思路">2. 代码结构和实现思路</h2>
<h3 id="tcpserver">TcpServer</h3>
<p>先实现HTTP的底层协议TCP的代码，也就是完成基本的网络套接字代码。实现成单例模式以供上层调用。</p>
<pre><code class="language-cpp">//TcpServer.hpp
#pragma once
#include &lt;iostream&gt;
#include &lt;string&gt;
#include &lt;cstdlib&gt;
#include &lt;cstring&gt;
#include &lt;sys/types.h&gt;
#include &lt;sys/socket.h&gt;
#include &lt;netinet/in.h&gt;
#include &lt;arpa/inet.h&gt;
#include &lt;pthread.h&gt;
#include &lt;fcntl.h&gt;

#define BACK_LOG 5

class TcpServer
{
private:
    int _port;
    int _listen_sock;

    TcpServer(int port) : _port(port), _listen_sock(-1)
    {}
    TcpServer(const TcpServer&amp; ts) = delete;
    TcpServer operator=(const TcpServer&amp; ts) = delete;

    static TcpServer* _svr;
public:

    static TcpServer* GetInstance(int port);
    void InitServer();

    void Socket();
    void Bind();
    void Listen();
    ~TcpServer()
    {}
};
TcpServer* TcpServer::_svr = nullptr;
</code></pre>
<h3 id="httpserver">HttpServer</h3>
<p>HttpServer类实现调用TcpServer单例，并进入事件循环。</p>
<pre><code class="language-cpp">#pragma once
#include &lt;iostream&gt;
#include &lt;pthread.h&gt;
#include &quot;TcpServer.hpp&quot;
#include &quot;Protocol.hpp&quot;

#define PORT 8080

class HttpServer
{
private:
    int _port;
    TcpServer* _tcp_svr;
    bool _stop;

public:
    HttpServer(int port = PORT) : _port(port), _tcp_svr(nullptr), _stop(false)
    {}

    void InitServer() {
        _tcp_svr = TcpServer::GetInstance(_port);
    }

    void Loop();
    
    ~HttpServer()
    {}
};
</code></pre>
<p>接下来就是，读取请求，分析请求，构建响应并返回。</p>
<h3 id="protocol">Protocol</h3>
<p>定制HTTP请求和响应的协议。接着就是<code>EndPoint</code>类实现HTTP读取解析响应等一系列函数。</p>
<pre><code class="language-cpp">struct HttpRequest
{
    std::string _request_line;
    std::vector&lt;std::string&gt; _request_headers;
    std::string _blank;
    std::string _request_body;

    std::string _method;
    std::string _uri;
    std::string _version;

    std::string _path;
    std::string _query;
    struct stat _resoucre_stat;
    bool _cgi = false;

    std::unordered_map&lt;std::string, std::string&gt; _header_kvs;
};

struct HttpResponse
{
    std::string _status_line;
    std::vector&lt;std::string&gt; _response_headers;
    std::string _blank = LINE_BREAK;
    std::string response_body;

    int _status_code = OK; 
    int _resource_fd = -1;
};
</code></pre>
<pre><code class="language-cpp">class EndPoint
{
private:
    int _sock;
    HttpRequest _http_request;
    HttpResponse _http_response;

private:
    void RecvHttpRequestLine();
    void RecvHttpRequestHeadler();

    void ParseHttpRequestLine();
    void ParseHttpRequestHeadler();
    void RecvParseHttpRequestBody();

	int ProcessCgi();
	int ProcessWebPage();

public:
    EndPoint(int sock) : _sock(sock) {}

    void RecvHttpRequest()
    {
        RecvHttpRequestLine();
        RecvHttpRequestHeadler();
    }
    void ParseHttpRequest()
    {
        ParseHttpRequestLine();
        ParseHttpRequestHeadler();
        RecvParseHttpRequestBody();
    }
	void BuildHttpResponse();
    void SendHttpResponse();

    ~EndPoint()
    {}
};
</code></pre>
<h4 id="读取和解析请求">读取和解析请求</h4>
<p>当前请求时，我们以行为单位。</p>
<p>从各大平台发来的数据的行分隔符各有不同，我们要做的是兼容所有情况，也就是我们要自行实现读取一行数据的接口。</p>
<pre><code class="language-cpp">void EndPoint::RecvHttpRequestLine()
{
    std::string&amp; line = _http_request._request_line;
    Util::ReadLine(_sock, &amp;line);
    line.resize(line.size() - 1);
}
void EndPoint::RecvHttpRequestHeadler()
{
    std::string line;
    while (true) // 读到空行，读取结束
    {
        Util::ReadLine(_sock, &amp;line);
        if (line == &quot;\n&quot;) {
            _http_request._blank = line; // 将空行放到blank中
            break;
        }
        line.resize(line.size() - 1);
        _http_request._request_headers.push_back(std::move(line));
        line.clear();
    }
}

void EndPoint::ParseHttpRequestLine()
{
    std::stringstream ss(_http_request._request_line);
    ss &gt;&gt; _http_request._method &gt;&gt; _http_request._uri &gt;&gt; _http_request._version;
}
void EndPoint::ParseHttpRequestHeadler()
{
    for (auto&amp; header : _http_request._request_headers)
        _http_request._header_kvs.insert(Util::GetKV(header, &quot;: &quot;));
}
void EndPoint::RecvParseHttpRequestBody()
{
    auto&amp; method = _http_request._method;
    auto&amp; headers_map = _http_request._header_kvs;
    auto&amp; body = _http_request._request_body;
    auto iter = headers_map.find(&quot;Content-Length&quot;);
    if (iter == headers_map.end())
        return;
    else
        _http_request._content_length = stoi(iter-&gt;second);
    Util::ReadLine(_sock, &amp;body);
    body.resize(body.size() - 1);
}
</code></pre>
<h4 id="构建和返回响应">构建和返回响应</h4>
<p>如果是GET方法获取资源路径，并进行一系列的检查判断。根据请求资源的类型设置CGI处理，如果是POST方法直接设置CGI。</p>
<pre><code class="language-cpp">void EndPoint::BuildHttpResponse()
{
    auto&amp; code = _http_response._status_code;
    auto&amp; path = _http_request._path;
    auto&amp; rsrc_st = _http_request._resoucre_stat;

    // 排除非法请求
    if (_http_request._method != &quot;GET&quot; &amp;&amp; _http_request._method != &quot;POST&quot;) {
        LOG(WARNING) &lt;&lt; &quot;bad request invaild method\n&quot;;
        code = BAD_REQUEST;
        goto END;
    }

    if (_http_request._method == &quot;GET&quot;) {
        size_t pos = _http_request._uri.find('?');
        if (pos != std::string::npos) {
            Util::CutString(_http_request._uri, &amp;path, &amp;_http_request._query, pos);
            _http_request._cgi = true; // 带参一定用cgi
        }
        else 
            path = _http_request._uri;

        // 检查资源路径
        path = WEB_ROOT + path; // 拼接web根目录前缀
        if (path.back() == '/')
            path += HOME_PAGE; // 拼接默认访问资源后缀

        //判断资源路径是否合法
        if (stat(path.c_str(), &amp;rsrc_st) == 0) {
            if (S_ISDIR(rsrc_st.st_mode)) {
                path += &quot;/&quot; + HOME_PAGE;
                stat(path.c_str(), &amp;rsrc_st);
            }
            if (rsrc_st.st_mode &amp; S_IXUSR ||
                rsrc_st.st_mode &amp; S_IXGRP ||
                rsrc_st.st_mode &amp; S_IXOTH)
                _http_request._cgi = true;
        }
        else {
            LOG(WARNING) &lt;&lt; &quot;require &quot; &lt;&lt; path + &quot; resource not found\n&quot;;
            code = NOT_FOUND;
            goto END;
        }
    }
    else if (_http_request._method == &quot;POST&quot;) {
        _http_request._cgi = true;
        path = WEB_ROOT + _http_request._uri;
    }
    else {}

    // 处理请求
    if (_http_request._cgi == true) {
        code = ProcessCgi();     // 执行cgi请求，程序运行结果放到response_body中
        if (code == OK)
            LOG(INFO) &lt;&lt; &quot;cgi process executed success\n&quot;;
        code = ProcessWebPage(); // 讲cgi结果构建网页返回
    }
    else {
        code = ProcessWebPage(); // 返回静态网页
        if (code == OK)
            LOG(INFO) &lt;&lt; &quot;send &quot; + path + &quot; success\n&quot;;
    }
}
</code></pre>
<p>发送响应就是简单的将构建好的响应返回给对端即可。</p>
<pre><code class="language-cpp">void EndPoint::SendHttpResponse()
{
    send(_sock, _http_response._status_line.c_str(), _http_response._status_line.size(), 0);
    for (auto&amp; header : _http_response._response_headers) {
        header += LINE_BREAK;
        send(_sock, header.c_str(), header.size(), 0);
    }
    send(_sock, _http_response._blank.c_str(), _http_response._blank.size(), 0);
    if (_http_request._cgi) {
        auto&amp; body = _http_response.response_body;
        int total = 0;
        while (total &lt; body.size()) {
            ssize_t s = send(_sock, body.c_str() + total, body.size() - total, 0);
            if (s == 0) break;
            total += s;
        }
    }
    else {
        sendfile(_sock, _http_response._resource_fd, 0, _http_request._resrc_stat.st_size);
        close(_http_response._resource_fd);
    }

    LOG(INFO) &lt;&lt; &quot;send http response success\n&quot;;
}
</code></pre>
<h4 id="处理静态和非静态请求">处理静态和非静态请求</h4>
<p>构建普通网页响应。</p>
<pre><code class="language-cpp">int ProcessNonCgi()
{
    _http_response._resource_fd = open(_http_request._path.c_str(), O_RDONLY); 
    if (_http_response._resource_fd &lt; 0)
        return NOT_FOUND;
    
    auto&amp; line = _http_response._status_line;
    auto&amp; code = _http_response._status_code;
    line = HTTP_VERSION + &quot; &quot; + std::to_string(code) + &quot; &quot; + Util::Code2Desc(code) + LINE_BREAK;
    
    auto&amp; stat = _http_request._resoucre_stat;
    auto&amp; path = _http_request._path;
    std::string content_type_header = &quot;Content-Type: &quot; + Util::Suffix2Type(GetSuffix(path));
    std::string content_length_header = &quot;Content-Length: &quot; + std::to_string(stat.st_size);
    std::string content_language_header = &quot;Content-Language: zh-cn&quot;;
    
    _http_response._response_headers.push_back(content_type_header);
    _http_response._response_headers.push_back(content_length_header);
    _http_response._response_headers.push_back(content_language_header);

    return OK;
}
</code></pre>
<p>构建CGI响应。这是本项目的重难点。</p>
<p>线程首先首先创建子进程，将具体执行进程程序替换的任务交给子进程。</p>
<p>其次定制父子进程通信协议。</p>
<p>请求方法，GET方法的请求参数，报头中的正文大小几个变量都用环境变量导给子进程。POST方法的请求体使用管道导给子进程。</p>
<p>中个细节代码中有注释说明。</p>
<pre><code class="language-cpp">int ProcessCgi()
{
    auto&amp; method = _http_request._method;
    auto&amp; body = _http_request._request_body;
    auto&amp; path = _http_request._path;
    auto&amp; code = _http_response._status_code;

    // 构建两个管道，一个是父写子读，一个是父读子写，管道从父进程角度命名
    // parent output[1]  --&gt;  output[0] child
    // parent  input[0]  &lt;--  input[1]  child
    int input[2];  // 父读子写
    int output[2]; // 父写子读
    if (pipe(input) &lt; 0 || pipe(output) &lt; 0)
        return SVR_ERROR;

    pid_t pid = fork();
    if (pid == 0) /* child */ {
        close(input[0]);
        close(output[1]);

        int ret = setenv(&quot;METHOD&quot;, method.c_str(), 1); // 先导请求方法
        if (method == &quot;GET&quot;) // 再导GET请求参数
            ret |= setenv(&quot;QUERY&quot;, _http_request._query.c_str(), 0); 
        else if (method == &quot;POST&quot;) // 再导正文大小
            ret |= setenv(&quot;CONTENT_LENGTH&quot;, std::to_string(body.size()).c_str(), 0); 

        if (ret &lt; 0)
            LOG(WARNING) &lt;&lt; &quot;set env failed, errno: &quot; &lt;&lt; errno &lt;&lt; '\n';

        dup2(input[1], 1);
        dup2(output[0], 0);

        execl(_http_request._path.c_str(), _http_request._path.c_str(), nullptr);
        code = SVR_ERROR;
        exit(1);
    }
    else if (pid &gt; 0) /* parent */ {
        close(input[1]);
        close(output[0]);

        if (_http_request._method == &quot;POST&quot;) {
            auto&amp; body = _http_request._request_body;
            int already = 0;
            while (already &lt; body.size()) {
                ssize_t s = write(output[1], body.c_str() + already, body.size() - already);
                if (s == 0) break;
                already += s;
            }
        }

        int status = 0;
        pid_t ret = waitpid(pid, &amp;status, 0);
        if (ret &lt; 0)
            LOG(ERROR) &lt;&lt; &quot;parent process wait failed\n&quot;;
            code = SVR_ERROR;
        }
        close(input[0]);
        close(output[1]);
        return code;
    }
    else { /* pid &lt; 0 */ 
        LOG(ERROR) &lt;&lt; &quot;failed to create child process\n&quot;;
        return SVR_ERROR;
    }

    return code;
}
</code></pre>
<h3 id="差错处理">差错处理</h3>
<p>在读取请求构建响应发送响应的过程中，都穿插着错误判断，并以HTTP响应状态码作为返回值。</p>
<p>在适当的地方<code>goto END;</code>，直接进行错误处理。错误处理利用得到的状态码构建错误响应，也就是返回错误网页，如404页面。</p>
<pre><code class="language-cpp">void BuildHttpResponse()
{
    // 排除非法请求
    if (_http_request._method != &quot;GET&quot; &amp;&amp; _http_request._method != &quot;POST&quot;)
    {
        LOG(WARNING) &lt;&lt; &quot;bad request invaild method\n&quot;;
        code = BAD_REQUEST;
        goto END;
    }
    
	//...
    
    // 差错处理
    END:
    if (code != OK)
    {
        LOG(INFO) &lt;&lt; &quot;headler error begin, code: &quot; &lt;&lt; code &lt;&lt; '\n';
        ErrorHelper(); // 构建错误响应
    }
}

private:
void ErrorHelper()
{
    _http_request._cgi = false; // 错误处理，返回静态网页
    auto&amp; code = _http_response._status_code;

    switch (code)
    {
        case BAD_REQUEST:
            HeadlerWrong(PAGE_404);
            break;
        case NOT_FOUND:
            HeadlerWrong(PAGE_404); // 单独构建404页面
            break;
        case SVR_ERROR:
            HeadlerWrong(PAGE_404);
            break;
        case SVR_UNAVL:
            HeadlerWrong(PAGE_404);
            break;
        default:
            LOG(WARNING) &lt;&lt; &quot;unkown error code&quot; &lt;&lt; std::endl;
            break;
    }
}

void HeadlerWrong(const std::string&amp; wrong_page)
{
    _http_request._path = WEB_ROOT + '/' + wrong_page;
    stat(_http_request._path.c_str(), &amp;_http_request._resoucre_stat);
    ProcessWebPage(); // 返回404页面
}

int ProcessCgi()
{
 	if (pid == 0) {
        //...
    }
    else if (pid &gt; 0) { /* parent */
        // 获取子进程退出结果
        int status = 0;
        pid_t ret = waitpid(pid, &amp;status, 0);
        if (ret == pid)
        {
            // 管道读取子进程输出
            char ch = 0;
            while (read(input[0], &amp;ch, 1))
                _http_response.response_body.push_back(ch); // 子进程输出放到响应体中

            //判断进程是否正常终止
            if (WIFEXITED(status)) {
                LOG(INFO) &lt;&lt; &quot;subprocess exited exit code: &quot; &lt;&lt; WEXITSTATUS(status) &lt;&lt; '\n';
                if (WEXITSTATUS(status) != 0)
                    code = BAD_REQUEST;
            } else {
                LOG(INFO) &lt;&lt; &quot;subprocess exited by signal: &quot; &lt;&lt; WIFEXITED(status) &lt;&lt; '\n';
                code = BAD_REQUEST;
            }
        }
        else {
            LOG(ERROR) &lt;&lt; &quot;parent process wait failed\n&quot;;
            code = SVR_ERROR;
        }
        //...

        return code;
    }
    else /* pid &lt; 0 */ {
        LOG(ERROR) &lt;&lt; &quot;failed to create child process\n&quot;;
        return SVR_ERROR;
    }

    return code;
}
</code></pre>
<h3 id="引入线程池">引入线程池</h3>
<p>任务队列中的任务类，设置回调方法，使任务体能够自行调用处理任务的函数。</p>
<pre><code class="language-cpp">class Task
{
private:
    int _sock;
    CallBack HandlerTask; // 设置回调 当队列中有任务时，调用回调让后端处理任务

public:
    Task() {}
    Task(int sock) : _sock(sock) {}
    ~Task() {}

    void ProcessTask() {
        HandlerTask(_sock);
    }
};
</code></pre>
<p>将<code>HeaderRequest</code>方法，构建成回调<code>CallBack</code>仿函数。</p>
<pre><code class="language-cpp">class CallBack
{
public:
    CallBack() {}
    ~CallBack() {}

public:
    void operator()(int sock) {
        EndPoint* ep = new EndPoint(sock);
        if (ep-&gt;RecvHttpRequest() &amp;&amp; ep-&gt;ParseHttpRequest()) {
            ep-&gt;BuildHttpResponse();
            ep-&gt;SendHttpResponse();
        }
        else 
            LOG(WARNING) &lt;&lt; &quot;recv http request failed\n&quot;;

        delete ep;
    }
};
</code></pre>
<p>设置线程池，并配备任务队列。</p>
<p>交给外部<code>HTTPServer</code>类向任务队列中添加<code>accept</code>接受到的任务。</p>
<p>自身设置<code>TASK_NUM</code>数量的线程来同步互斥地获取任务队列中的任务。</p>
<pre><code class="language-cpp">class ThreadPool
{
private:
    std::queue&lt;Task&gt; _task_queue;
    int _task_num;
    bool _stop;

    pthread_mutex_t _mtx;
    pthread_cond_t _cond;
    static ThreadPool* _thread_pool;

private:
    ThreadPool() = default;
    ThreadPool(int num = TASK_NUM) : _task_num(num), _stop(false) {
        pthread_mutex_init(&amp;_mtx, nullptr);
        pthread_cond_init(&amp;_cond, nullptr);
    }
    ThreadPool(const ThreadPool&amp;) = delete;

public:
    static ThreadPool* GetInstance() {
        pthread_mutex_t lck = PTHREAD_MUTEX_INITIALIZER;
        if (_thread_pool == nullptr) {
            pthread_mutex_lock(&amp;lck);
            if (_thread_pool == nullptr) {
                _thread_pool = new ThreadPool(TASK_NUM);
                _thread_pool-&gt;InitThreadPool();
            }
            pthread_mutex_unlock(&amp;lck);
        }
        return _thread_pool;
    }

    void InitThreadPool() {
        for (int i = 0; i &lt; _task_num; ++i) {
            pthread_t tid;
            if (pthread_create(&amp;tid, nullptr, ThreadRoutine, (void*)_thread_pool) != 0) {
                LOG(FATAL) &lt;&lt; &quot;create pthread failed\n&quot;;
                exit(1);
            }
        }
        LOG(INFO) &lt;&lt; &quot;thread pool init success\n&quot;;
    }

    bool isEmpty() {
        return _task_queue.empty();
    }
    void Lock() {
        pthread_mutex_lock(&amp;_mtx);
    }
    void Unlock() {
        pthread_mutex_unlock(&amp;_mtx);
    }
    void ThreadWait() {
        pthread_cond_wait(&amp;_cond, &amp;_mtx);
    }
    void ThreadWakeup() {
        pthread_cond_signal(&amp;_cond);
    }

    static void* ThreadRoutine(void* args) {
        ThreadPool* tp = (ThreadPool*)args;
        while (!tp-&gt;_stop) {
            tp-&gt;Lock();

            while (tp-&gt;isEmpty())
                tp-&gt;ThreadWait();

            Task task;
            tp-&gt;PopTask(&amp;task);

            tp-&gt;Unlock();
            task.ProcessTask();
        }
    }

    void PushTask(const Task&amp; task) {
        Lock();

        _task_queue.push(task);

        Unlock();
        ThreadWakeup();
    }

    void PopTask(Task* task) {
        *task = _task_queue.front();
        _task_queue.pop();
    }

    ~ThreadPool() {
        pthread_mutex_destroy(&amp;_mtx);
        pthread_cond_destroy(&amp;_cond);
    }
};
ThreadPool* ThreadPool::_thread_pool = nullptr;
</code></pre>
<pre><code class="language-cpp">class HttpServer
{
private:
    int _port;
    TcpServer* _tcp_svr;
    ThreadPool* _thread_pool;
    bool _stop;

public:
    HttpServer(int port = PORT) : _port(port), _tcp_svr(nullptr), _stop(false) {}
    ~HttpServer() {}
    
    void InitServer() {
        signal(SIGPIPE, SIG_IGN); // 忽略SIGPIPE信号
        _tcp_svr = TcpServer::GetInstance(_port);
        _thread_pool = ThreadPool::GetInstance();
    }

    void Loop() {
        while (!_stop) {
            struct sockaddr_in peer;
            socklen_t len = sizeof(peer);
            int sock = accept(_tcp_svr-&gt;GetListenSock(), (struct sockaddr*)&amp;peer, &amp;len);
            if (sock &lt; 0) continue;

            Task task(sock);
            _thread_pool-&gt;PushTask(task);
        }
    }
};
</code></pre>
<h3 id="调用逻辑">调用逻辑</h3>
<img src="https://img.gejiba.com/images/f6f73baa5fd26e94615388c1a585f610.png" alt="httpserver调用逻辑" border="0" style="zoom:50%;" >
<p> </p>
<h2 id="3-难点总结和项目扩展">3. 难点总结和项目扩展</h2>
<p>对于CGI机制的理解和实现。</p>
<img src="https://img.gejiba.com/images/60227f51c300c20f1f1b5879101d5270.png" alt="HTTP服务流程" border="0">
<p>整个HTTP服务就是CGI程序和客户端沟通的桥梁，因为CGI程序与外界的输入输出都由HTTP服务器代理和转发。</p>
<p>通过子进程进程程序替换的方式，能够调用任意程序，且可以获得程序的运行结果和控制其输入输出。</p>
<h3 id="项目扩展">项目扩展</h3>
<ul>
<li>URL encode decode</li>
<li>数据库增删查改</li>
<li>HTTP其他方法</li>
<li>配置文件化</li>
<li>301302转发</li>
</ul>
<h4 id="业务层面">业务层面</h4>
<ul class="contains-task-list">
<li class="task-list-item"><input class="task-list-item-checkbox" disabled="" type="checkbox" id="task-item-3596081"><label class="task-list-item-label" for="task-item-3596081"> 实现在线计算器（日期转换等）</label></li>
<li class="task-list-item"><input class="task-list-item-checkbox" checked="" disabled="" type="checkbox" id="task-item-1354789"><label class="task-list-item-label" for="task-item-1354789"> 实现在线简历</label></li>
<li class="task-list-item"><input class="task-list-item-checkbox" disabled="" type="checkbox" id="task-item-8957209"><label class="task-list-item-label" for="task-item-8957209"> 实现博客系统</label></li>
</ul>
<h4 id="技术层面">技术层面</h4>
<ul class="contains-task-list">
<li class="task-list-item"><input class="task-list-item-checkbox" disabled="" type="checkbox" id="task-item-1472570"><label class="task-list-item-label" for="task-item-1472570"> 支持HTTP1.1长连接（链接管理）</label></li>
<li class="task-list-item"><input class="task-list-item-checkbox" disabled="" type="checkbox" id="task-item-8456324"><label class="task-list-item-label" for="task-item-8456324"> 提高并发量和执行效率</label></li>
<li class="task-list-item"><input class="task-list-item-checkbox" disabled="" type="checkbox" id="task-item-9213280"><label class="task-list-item-label" for="task-item-9213280"> 支持redis</label></li>
<li class="task-list-item"><input class="task-list-item-checkbox" disabled="" type="checkbox" id="task-item-6075945"><label class="task-list-item-label" for="task-item-6075945"> 支持多机器业务转发负载均衡的代理功能</label></li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[BoostSearch搜索引擎]]></title>
        <id>https://yourfriendyo.github.io/post/boostsearch-sou-suo-yin-qing/</id>
        <link href="https://yourfriendyo.github.io/post/boostsearch-sou-suo-yin-qing/">
        </link>
        <updated>2023-01-14T18:19:11.000Z</updated>
        <content type="html"><![CDATA[<h1 id="boostsearch搜索引擎">BoostSearch搜索引擎</h1>
<h2 id="1-项目的相关背景">1. 项目的相关背景</h2>
<blockquote>
<p>研发搜索引擎的公司，如百度、搜狗、360搜索，还有各大网站各种客户端也提供搜索功能。</p>
<p>Boost官方网站是没有搜索功能的，所以我们为Boost实现一个站内搜索引擎。</p>
</blockquote>
<p>自行实现一个全网搜索引擎难度极大，是十分困难的，但是实现站内搜索，也就是只搜索网站内的内容，这样搜索的内容更垂直，数据量更小，也可以达到以小见大的效果。</p>
<p>对于搜索结果，基本包含三个部分：网页标题、网页内容摘要、目标网页地址。</p>
<img src="https://img.gejiba.com/images/f29d5b97db5c9f1445096530f5fc4630.png" alt="搜索结果组成图示" border="0" style="zoom: 50%;" >
<blockquote>
<p>对于图片，视频等的展示，暂不考虑。</p>
</blockquote>
<h2 id="2-搜索引擎的相关宏观原理">2. 搜索引擎的相关宏观原理</h2>
<img src="https://img.gejiba.com/images/20cfb315081a44ba6f0e501ef973ba68.png" alt="搜索引擎宏观实现原理" border="0" style="zoom: 50%;" >
<h2 id="3-相关技术栈和项目环境">3. 相关技术栈和项目环境</h2>
<h3 id="31-技术栈">3.1 技术栈</h3>
<ul>
<li>后端：C/C++，C++11，STL，Boost标准库，Jsoncpp，cppjieba，cpp-httplib，</li>
<li>前端：html5，jQuery，Ajax</li>
</ul>
<h3 id="32-项目环境">3.2 项目环境</h3>
<p>Centos7云服务器，vim/gcc(g++)/Makefile，vscode</p>
<p> </p>
<h2 id="4-搜索引擎的具体原理">4. 搜索引擎的具体原理</h2>
<h3 id="41-正排索引和倒排索引">4.1 正排索引和倒排索引</h3>
<p>搜索引擎必然要对内容建立索引，才能更快的搜索和返回。有两种索引：正排索引和倒排索引。以如下内容举例：</p>
<blockquote>
<p>有如下两个文档，我们对这两个文档内容建立索引：</p>
<ul>
<li>文档1：雷军买了四斤小米</li>
<li>文档2：雷军发布了小米手机</li>
</ul>
</blockquote>
<p>建立索引本质就是建立文档内容和文档ID的关系。</p>
<h4 id="正排索引">正排索引</h4>
<table>
<thead>
<tr>
<th>文档ID</th>
<th>文档内容</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>雷军买了四斤小米</td>
</tr>
<tr>
<td>2</td>
<td>雷军发布了小米手机</td>
</tr>
</tbody>
</table>
<p><strong>正排索引就是从文档ID找到文档内容（文档内的关键字）</strong>。</p>
<h4 id="分词">分词</h4>
<p>我们拿到文档首先要对其编号，其次<strong>对文档内容进行分词，也就是得到文档内的关键字，为的是建立倒排索引和方便查找</strong>。</p>
<ul>
<li>文档1：雷军买了四斤小米：雷军/买/了/四斤/小米/四斤小米</li>
<li>文档2：雷军发布了小米手机：雷军/发布/了/小米/小米手机</li>
</ul>
<blockquote>
<p>其中对于“了”、“呢”、“的”、“啊”，这些词都被称为停止词或暂停词。这些词对我们建立索引是没有意义的，在分词的时候都会被去掉。</p>
</blockquote>
<h4 id="倒排索引">倒排索引</h4>
<p>倒排索引就是根据文档内容，进行分词，整理具有唯一性不重复的关键字。再根据关键字找到关联文档ID的方案。</p>
<table>
<thead>
<tr>
<th>关键字（具有唯一性）</th>
<th>文档ID</th>
</tr>
</thead>
<tbody>
<tr>
<td>雷军</td>
<td>1，2</td>
</tr>
<tr>
<td>买</td>
<td>1</td>
</tr>
<tr>
<td>四斤</td>
<td>1</td>
</tr>
<tr>
<td>小米</td>
<td>1，2</td>
</tr>
<tr>
<td>四斤小米</td>
<td>1</td>
</tr>
<tr>
<td>发布</td>
<td>2</td>
</tr>
<tr>
<td>小米手机</td>
<td>2</td>
</tr>
</tbody>
</table>
<p>模拟一次查找过程：</p>
<ol>
<li>用户输入：小米</li>
<li><strong>在倒排索引中查找，提取出文档ID (1, 2)</strong></li>
<li><strong>根据正排索引，找到文档内容</strong></li>
<li>获取文档的标题、内容、描述、URL</li>
<li>对文档结果进行摘要</li>
<li>构建响应并返回</li>
</ol>
<blockquote>
<p>大搜索引擎会对文档附加权重属性，以决定该文档内容展示的优先级。</p>
</blockquote>
<h3 id="42-认识标签和去标签">4.2 认识标签和去标签</h3>
<p>https://www.boost.org</p>
<p>将 boost_1_78_0/doc/html 中的数据作为需要建立索引的数据源。</p>
<h4 id="去标签">去标签</h4>
<p>现在我们首先将数据源中的各个文档去标签化。HTML 是标签化语言，所有的语句都被一对标签包裹起来，由左右尖括号括起来的就是标签，对数据本身是无意义的，所以我们首先要将其去掉。</p>
<p>一般标签都是成对出现的，标签中的属性信息也是不需要的，只有标签内的数据是有用数据。</p>
<pre><code class="language-html">&lt;title&gt;Chapter 37. Boost.STLInterfaces&lt;/title&gt;

&lt;link rel=&quot;stylesheet&quot; href=&quot;../../doc/src/boostbook.css&quot; type=&quot;text/css&quot;&gt;

&lt;td align=&quot;center&quot;&gt;&lt;a href=&quot;../../index.html&quot;&gt;Home&lt;/a&gt;&lt;/td&gt;
</code></pre>
<p>目标是：把每个文档都进行去标签，清洗后写到同一个文件中，每个文档的内容用<code>\3</code>分隔。<code>\3</code>是不可显的控制字符，不会污染净化后的数据。</p>
<p> </p>
<h2 id="5-数据清洗的模块">5. 数据清洗的模块</h2>
<h3 id="51-大致框架">5.1 大致框架</h3>
<p>数据清洗模块框架大致如此：</p>
<ol>
<li>遍历网页文件目录下的所有html文件获取上来并进行解析，</li>
<li>把解析后的数据放到清洗后数据存放位置。</li>
</ol>
<pre><code class="language-cpp">const std::string src_path = &quot;data/input/&quot;; // html网页数据源路径
const std::string output = &quot;data/output/raw.bin&quot;; // 文档数据清洗保存路径

bool EnumFiles(const std::string&amp; src_path, std::vector&lt;std::string&gt;* file_list);
bool ParseFiles(std::vector&lt;std::string&gt;&amp; file_list, std::vector&lt;DocInfo&gt;* parser_res);
bool SaveFiles(const std::vector&lt;DocInfo&gt;&amp; parser_res, const std::string&amp; output);
// 获取到的文件，解析其相关属性
struct DocInfo {
    std::string title;       // 文档标题
    std::string content;     // 文档内容
    std::string url;         // 文档地址
};
int main()
{
    std::vector&lt;std::string&gt; file_list; // 保存所有文件名
    // 递归遍历所有数据源文件，将文件名保存到file_list中，以便后期读取
    if (!EnumFiles(src_path, &amp;file_list)) {
        std::cerr &lt;&lt; &quot;enum file error&quot; &lt;&lt; std::endl;
        exit(1);
    }
    // 读取每个文件的内容，并进行解析
    std::vector&lt;DocInfo&gt; parser_res;
    if (ParseFiles(file_list, &amp;parser_res)) {
        std::cerr &lt;&lt; &quot;enum file error&quot; &lt;&lt; std::endl;
        exit(2);
    }
    // 把解析得到的内容写入到output中，以\3作为每个文档内容的分隔符
    if (!SaveFiles(parser_res, output)) {
        std::cerr &lt;&lt; &quot;save file error&quot; &lt;&lt; std::endl;
        exit(3);
    }
    return 0;
}
</code></pre>
<p>C++库对文件系统的支持并不是很好，所以我们采用boost库的文件操作API。</p>
<blockquote>
<p>centos安装boost开发库：</p>
<pre><code class="language-shell">$ sudo yum install -y  boost-devel
</code></pre>
</blockquote>
<h3 id="52-遍历文件">5.2 遍历文件</h3>
<pre><code class="language-cpp">bool EnumFiles(const std::string&amp; src_path, std::vector&lt;std::string&gt;* file_list)
{
    namespace fs = boost::filesystem;
    fs::path root_path(src_path); // 定义路径对象
    if (!fs::exists(root_path)) {
        std::cerr &lt;&lt; src_path &lt;&lt; &quot; is not exists&quot; &lt;&lt; std::endl;
        return false;
    }
    // 定义对象迭代器对象
    fs::recursive_directory_iterator end;
    for (fs::recursive_directory_iterator iter(root_path); iter != end; iter++)
    {
        if (!fs::is_regular_file(*iter)) {            // 判断是否为普通文件
            continue;
        }
        if (iter-&gt;path().extension() != &quot;.html&quot;) {    // 判断后缀是否为html
            continue;
        } // 当前的路径一定是一个合法文件路径

        file_list-&gt;push_back(iter-&gt;path().string());  // 文件路径放到file_list中
    }
    return true;
}
</code></pre>
<h3 id="53-解析文件">5.3 解析文件</h3>
<h4 id="大致框架">大致框架</h4>
<p>先读取文件，再依次解析文件的 title、content、url，解析成功后拷贝至解析结果数组中。</p>
<pre><code class="language-cpp">bool ParseFiles(const std::vector&lt;std::string&gt;&amp; file_list, std::vector&lt;DocInfo&gt;* parser_res)
{
    for (auto&amp; file : file_list)
    {
        //读取文件
        std::string result; // 读取的结果
        if (!Util::ReadFile(file, &amp;result)) {
            continue;
        }
        // 解析文件
        DocInfo info;
        // title
        if (!ParserTitle(result, &amp;info._title)) {
            continue;
        }
        // content
        if (!ParserContent(result, &amp;info._content)) {
            continue;
        }
        // url
        if (!ParserUrl()) {
            continue;
        }
        // 当前文档解析完毕，属性在info中，导入parser_res
        parser_res-&gt;push_back(info); // 拷贝，低效
    }
    return true;
}
</code></pre>
<h4 id="提取title">提取title</h4>
<pre><code class="language-html">&lt;title&gt;Redirect to generated documentation&lt;/title&gt;
</code></pre>
<pre><code class="language-cpp">static bool ParserTitle(const std::string&amp; file, std::string* title)
{
    std::size_t begin = file.find(&quot;&lt;title&gt;&quot;); // 查找字符串&lt;title&gt;
    if (begin == std::string::npos) {
        return false;
    }
    std::size_t end = file.find(&quot;&lt;/title&gt;&quot;); // 查找字符串&lt;/title&gt;
    if (end == std::string::npos) {
        return false;
    }

    begin += std::string(&quot;&lt;title&gt;&quot;).size(); // 指向title位置
    if (begin &gt; end)
        return false;
    
    *title = std::string(begin, end);

    std::cout &lt;&lt; &quot;title: &quot; &lt;&lt; title &lt;&lt; std::endl;

    return true;
}
</code></pre>
<h4 id="提取content">提取content</h4>
<p>遍历html文件内容时，只要碰到<code>&gt;</code>就意味着当前标签被处理完毕，只要碰到<code>&lt;</code>就意味着即将处理新标签。</p>
<p>用枚举类型描述这俩种状态，条件就绪更改状态，遇到内容时就插入到对应字符串中。</p>
<pre><code class="language-cpp">bool ParserContent(const std::string&amp; file, std::string* content)
{
    //去标签，基于一个简易的状态机编写
    enum STATUS {
        LABLE,
        CONTENT
    };
    enum STATUS s = LABLE;
    for (char c : file) {
        switch (s) {
        case LABLE:
            if (c == '&gt;') s = CONTENT;   // 可能进入读内容模式
            break;
        case CONTENT:
            if (c == '&lt;') s = LABLE;     // 可能进入读标签模式
            else {
                // 处理掉原始内容中的\n，将其留作html解析之后的文本分隔符
                if (c == '\n') c = '\0';
                content-&gt;push_back(c);   //
            }
            break;
        default:
            std::cout &lt;&lt; &quot;unkown status&quot; &lt;&lt; std::endl;
            break;
        }
    }
    return true;
}
</code></pre>
<h4 id="构建url">构建URL</h4>
<p>boost库的官方文档和我们项目中的文档数据源，当然是可以对应起来的。构建一下即可。</p>
<pre><code class="language-html">https://www.boost.org/doc/libs/1_80_0/doc/html/*
/home/yyx/Project/BoostSearch/data/input/*
</code></pre>
<pre><code class="language-cpp">bool ParserUrl(const std::string&amp; file, std::string* url)
{
    std::string url_head = &quot;https://www.boost.org/doc/libs/1_80_0/doc/html/&quot;; // 构建前缀

    int begin = file.rfind('/');
    if (begin == std::string::npos) {
        std::cout &lt;&lt; &quot;file suffix find error&quot; &lt;&lt; std::endl;
        return false;
    }

    std::string url_tail(file, begin + 1);
    *url = url_head + url_tail;

    return true;
}
</code></pre>
<h3 id="54-数据保存">5.4 数据保存</h3>
<blockquote>
<p>之前我们我们确定的解析目标是将所有文件的属性信息（title、content、url）保存在<code>unq.bin</code>中，每个文档的属性以<code>\3</code>分隔。</p>
</blockquote>
<pre><code class="language-cpp">title\ncontent\nurl\n \3 title\ncontent\nurl\n \3 title\ncontent\nurl\n \3
</code></pre>
<p>但为使用<code>getline</code>一次读取一行能够直接获得一个文档的所有属性信息，我们现将分隔符修改一下：</p>
<pre><code class="language-cpp">title\3content\3url \n title\3content\3url \n title\3content\3url \n 
</code></pre>
<pre><code class="language-cpp">// 保存文件
bool SaveFiles(const std::vector&lt;DocInfo&gt;&amp; parser_res, const std::string&amp; output)
{
#define SEP '\3'
    std::ofstream ofs(output, std::ios::out | std::ios::binary); // 以二进制形式写入
    if (!ofs.is_open()) {
        std::cerr &lt;&lt; &quot;open &quot; &lt;&lt; output &lt;&lt; &quot; failed&quot; &lt;&lt; std::endl;
        return false;
    }
    // 遍历数据
    for (auto&amp; e : parser_res)
    {
        std::string out_string(e._title + SEP + e._content + SEP + e._url + '\n');
        ofs.write(out_string.c_str(), out_string.size()); //写入文件
    }
    ofs.close();

    return true;
}
</code></pre>
<img src="https://img.gejiba.com/images/a5228901ccb6ac7bf860ae7d9ec3c094.png" border="0">
<blockquote>
<p>结果如图所示：属性之间的分隔符是<code>\3</code>显示为<code>^C</code>。文档之间的分隔符是<code>\n</code>不显示。</p>
</blockquote>
<p> </p>
<h2 id="6-建立索引的模块">6. 建立索引的模块</h2>
<h3 id="61-大体框架">6.1 大体框架</h3>
<h4 id="正排索引-2">正排索引</h4>
<p>正排索引是建立文档ID和内容之间的联系所以用一个结构体<code>DocInfo</code>就可以存放相关信息。</p>
<pre><code class="language-cpp">struct DocInfo
{
    std::string _title;    // 标题
    std::string _content;  // 内容
    std::string _url;      // 地址
    uint64_t _doc_id;      // 文档ID
};
</code></pre>
<p>保存的数据结构我们采用数组即可，使用数组的下标作为文档ID。</p>
<pre><code class="language-cpp">// 正排索引的数据结构使用数组，用下标表示文档ID
std::vector&lt;DocInfo&gt; forward_index;  // 正排索引
</code></pre>
<h4 id="倒排索引-2">倒排索引</h4>
<p>倒排索引建立关键字和多个其所在文档的相关信息的映射，相关信息有文档ID、文档中所有的关键字、权重等有关信息。</p>
<p>我们把这个相关信息封装在结构体<code>InvertedElem</code>中：</p>
<pre><code class="language-cpp">struct InvertedElem
{
    int _doc_id;           // 文档ID
    std::string _word;     // 关键字
    int _weight;           // 权重
};
</code></pre>
<p>再通过哈希建立关键字和多个文档信息结构体的映射。我们将关键字所对应的多个文档信息结构体的集合叫做倒排拉链。</p>
<pre><code class="language-cpp">// 倒排拉链
typedef std::vector&lt;InvertedElem&gt; InvertedList;
// 倒排索引一定是一个关键字和一组InvertedElem对象对应
std::unordered_map&lt;std::string, InvertedList&gt; inverted_index; // 倒排索引
</code></pre>
<h4 id="索引结构">索引结构</h4>
<pre><code class="language-cpp">class Index {
    // 倒排拉链
    typedef std::vector&lt;InvertedElem&gt; InvertedList;
private:
    std::vector&lt;DocInfo&gt; forward_index;                           // 正排索引
    std::unordered_map&lt;std::string, InvertedList&gt; inverted_index; // 倒排索引
public:
    Index() {}
    ~Index() {}
    // 构建索引 -- 根据格式化后的文件名建立索引
    bool BuildIndex(const std::string&amp; file_path);
    
    // 正排索引 -- 根据doc_id获得文档内容
    DocInfo* GetDocInfo(uint64_t doc_id);
    
    // 倒排索引 -- 根据关键字word获得倒排拉链
    InvertedList* GetInvertedList(const std::string&amp; word);
};
</code></pre>
<ol>
<li>首先要建立索引，通过传递来的解析后的文档名，打开该文件并建立索引。</li>
<li>正排索引的本质是通过文档ID获得文档内容，也就是<code>GetDocInfo</code>。</li>
<li>倒排索引的本质是通过关键字获得对应的文档的相关信息，即倒排拉链，也就是<code>GetInvertList</code>。</li>
</ol>
<h3 id="62-具体实现">6.2 具体实现</h3>
<h4 id="正排倒排索引">正排倒排索引</h4>
<p>正排索引倒排索引都是查找并返回对应数据结构中的一个节点。</p>
<pre><code class="language-cpp">// 正排索引
DocInfo* GetDocInfo(uint64_t doc_id)
{
    if (doc_id &gt;= _forward_index.size()) {
        std::cerr &lt;&lt; &quot;doc_id error out of range&quot; &lt;&lt; std::endl;
        return nullptr;
    }
    return &amp;_forward_index[doc_id];
}
// 倒排索引
InvertedList* GetInvertedList(const std::string&amp; word)
{
    auto iter = _inverted_index.find(word);
    if (iter == _inverted_index.end()) {
        std::cerr &lt;&lt; &quot;get invertedlist failed, invaild word&quot; &lt;&lt; std::endl;
        return nullptr;
    }
    return &amp;(iter-&gt;second);
}
</code></pre>
<h4 id="构建索引">构建索引</h4>
<p>构建索引就是先获取到数据清洗之后的文件，把每个文档内容提取出来，建立正派和倒排索引。</p>
<pre><code class="language-cpp">// 构建索引 -- 根据格式化后的文件名建立索引
bool BuildIndex(const std::string&amp; file_path)
{
    std::ifstream ifs(file_path, std::ios::in | std::ios::binary); // 打开解析后的文件
    if (!ifs.is_open()) {
        std::cerr &lt;&lt; &quot;build index error, open &quot; &lt;&lt; file_path &lt;&lt; &quot; failed&quot; &lt;&lt; std::endl;
        return false;
    }
    std::string line; // 每个文件的内容占一行
    while (getline(ifs, line)) {
        DocInfo* info = BuildForwardIndex(line); // 建立正排索引
        if (info == nullptr) {
            std::cerr &lt;&lt; &quot;build line error, continue&quot; &lt;&lt; std::endl;  // for debug
            continue;
        }
        BuildInvertedIndex(*info); // 构建倒排索引
    }
    return true;
}
</code></pre>
<h5 id="建立正派索引">建立正派索引</h5>
<p>首先是建立正派索引，提取出每个文档的信息我们先划分出标题、内容、URL，再添加上文档ID一并打包放到正派索引数组中。</p>
<pre><code class="language-cpp">private:
// 建立正排索引
DocInfo* BuildForwardIndex(const std::string&amp; line)
{
    // 解析line字符串
    std::string sep(&quot;\3&quot;); // 行内分隔符
    std::vector&lt;std::string&gt; info_result; // 单行解析结果存储位置
    Util::CutString(line, &amp;info_result, sep);

    if (info_result.size() != 3) {
        return nullptr;
    }
    // 切分后放到Docinfo结构体
    DocInfo info;
    info._title = info_result[0];
    info._content = info_result[1];
    info._url = info_result[2];
    info._doc_id = _forward_index.size();
    // 放到正排数组vector中
    _forward_index.push_back(std::move(info));
    return &amp;(_forward_index.back());
}
</code></pre>
<h5 id="构建倒排索引">构建倒排索引</h5>
<p>然后就是构建倒排索引，提取出文档信息后，</p>
<ol>
<li>将文档的标题、内容拿出来，进行分词，分出多个关键词；</li>
<li>在遍历分出来的关键字，进行词频统计，计算词和文档的相关性；</li>
</ol>
<blockquote>
<p>分词工具我们采用jieba分词工具。需要将deps下的limonp和arts-clone目录拷贝到include/jieba中。 github上的limonp目录在另一个项目需要单独下载再组合。</p>
<p>使用示例：</p>
</blockquote>
<img src="https://img.gejiba.com/images/bb87a302e9bfb1a8c0e90aa5682c91fe.png" style="zoom:50%;" >
<ol>
<li>先建立词频统计的结构体，再建立词和词频结构体的映射表。</li>
<li>针对标题和内容分别分词，再分别对每个词进行词频统计，统计到映射表中。</li>
<li>创建并填充一个倒排元素，再将倒排结构体添加到倒排拉链中。</li>
</ol>
<pre><code class="language-cpp">bool BuildInvertedIndex(const DocInfo&amp; info)
{
    // 词频统计结构体
    struct word_cnt // 针对一个词的数据统计
    {
        int _title_cnt;
        int _content_cnt;

        word_cnt() : _title_cnt(0), _content_cnt(0)
        {}
    };
    std::unordered_map&lt;std::string, word_cnt&gt; word_map; // 暂存词与词频的映射表

    /* 根据文档标题和内容，分词并进行词频统计 */

    // 针对标题分词
    std::vector&lt;std::string&gt; title_words;
    Jieba::CutString(info._title, &amp;title_words);
    // 遍历标题，进行词频统计
    for (std::string s : title_words) {
        boost::to_lower(s); // 统一转小写
        word_map[s]._title_cnt++; // 查找对应关键词，将标题次数++
    }

    // 针对内容分词
    std::vector&lt;std::string&gt; content_words;
    Jieba::CutString(info._content, &amp;content_words);
    // 遍历内容，进行词频统计
    for (std::string s : content_words) {
        boost::to_lower(s);
        word_map[s]._content_cnt++;
    }

    /* 建立word和倒排拉链的映射 */
#define TITLE_PRI 10
#define CONTENT_PRI 1
    for (auto&amp; word_pair : word_map)
    {
        InvertedElem item;
        // 构建元素
        item._doc_id = info._doc_id;
        item._word = word_pair.first;
        item._weight = TITLE_PRI * word_pair.second._title_cnt +
            CONTENT_PRI * word_pair.second._content_cnt; // 设置权重
        // 向拉链中添加元素
        _inverted_index[word_pair.first].push_back(item);
    }
    return true;
}
</code></pre>
<p> </p>
<h2 id="7-搜索引擎的模块">7. 搜索引擎的模块</h2>
<img src="https://img.gejiba.com/images/91a5f2607079bef0986e64353ac333eb.png" style="zoom:50%;"  >
<p>如图所示，搜索关键字也会被服务端分词，再进行索引查找，最后将所有结果返回给用户。</p>
<h3 id="71-大致结构">7.1 大致结构</h3>
<pre><code class="language-cpp">class Searcher
{
private:
    NS_Index::Index* index; // 供系统进行查找到索引
public:
    Searcher() {}
    ~Searcher() {}
    void InitSearcher(const std::string&amp; input)
    {
        // 获取index对象
        _index = NS_Index::Index::GetInstance();
        // 建立索引
        _index-&gt;BuildIndex(input);
    }
    
    // @param query 用户输入的数据
    // @param json_string 返回给浏览器的结果
    void Search(const std::string&amp; query, std::string* json_string);
    //...
};
</code></pre>
<h3 id="72-搜索功能">7.2 搜索功能</h3>
<p>参数很明确，用户数据的字符串和数据处理后返回的字符串。</p>
<ol>
<li>首先要对用户发来的字符串分词，分出多个关键词；</li>
<li>遍历多个关键词进行查找，将所有关键词对应的倒排拉链放到一个拉链中，并完成去重和排序工作；</li>
<li>遍历总倒排拉链，将其中对应文档信息的构建Json串，并返回。</li>
</ol>
<pre><code class="language-cpp">// @param query 用户输入的数据
// @param json_string 返回给浏览器的结果
void Search(const std::string&amp; query, std::string* json_string)
{
    /* 对query分词 */
    std::vector&lt;std::string&gt; words;
    Jieba::CutString(query, &amp;words);

    /* 对分出的多个关键词进行查找 */
    NS_Index::Index::InvertedList ivtd_list_v; // 保存所有关键字的所有倒排拉链

    for (auto word : words)
    {
        boost::to_lower(word);

        NS_Index::Index::InvertedList* ivtd_list = _index-&gt;GetInvertedList(word);
        if (ivtd_list == nullptr) {
            continue;
        }
        // 汇总到总倒排拉链中
        ivtd_list_v.insert(ivtd_list_v.end(), ivtd_list-&gt;begin(), ivtd_list-&gt;end());
    }

    /* 汇总所有查找结果，按照权重降序排序 */
    // 去重
    std::set&lt;NS_Index::InvertedElem&gt; unique_set(ivtd_list_v.begin(), ivtd_list_v.end());
    ivtd_list_v.assign(unique_set.begin(), unique_set.end()); // 导入去重后结果

    // 降序排序
    std::sort(ivtd_list_v.begin(), ivtd_list_v.end(),
              [](const NS_Index::InvertedElem&amp; e1, const NS_Index::InvertedElem&amp; e2) {
                  return e1._weight &gt; e2._weight;
              });

    /* 将整体查找结果构建json序列化并返回 */
    // json对象
    Json::Value root;

    // 获取关键字对应文档信息
    for (auto&amp; item : ivtd_list_v)
    {
        NS_Index::DocInfo* info = _index-&gt;GetDocInfo(item._doc_id);
        if (info == nullptr) {
            continue;
        }

        // 序列化
        Json::Value elem;
        elem[&quot;title&quot;] = info-&gt;_title;
        elem[&quot;desc&quot;] = GetDesc(info-&gt;_content, item._word);
        elem[&quot;url&quot;] = info-&gt;_url;

        root.append(elem);
    }

    Json::StyledWriter writer;
    *json_string = writer.write(root); // 获取json串
}
</code></pre>
<h4 id="获取摘要">获取摘要</h4>
<pre><code class="language-cpp">std::string GetDesc(std::string&amp; content, const std::string&amp; word)
{
    // 找到首次出现位置
    size_t pos = content.find(word);
    if (pos == std::string::npos)
        return &quot;NONE&quot;;

    // 确定开始和结束位置
    const size_t prev_step = 50; // 向前向后步长
    const size_t next_step = 50;

    size_t start = 0; // 起始结束默认值
    size_t end = content.size() - 1;

    // 从pos位置先前向后扩展step长度
    if (pos - prev_step &gt;= 0)
        start = pos - prev_step;
    if (pos + next_step &lt;= content.size())
     	end = pos + next_step;

    // 截取子串并返回
    return std::string(start, end - start + 1);
}
</code></pre>
<p><img src="https://img.gejiba.com/images/c1a88cb30fd1e82dd3a356b7a4dc0918.png"/> </p>
<h3 id="73-问题修复">7.3 问题修复</h3>
<h4 id="无符号数错误">无符号数错误</h4>
<p>使用无符号数进行存在负数的比较判断本身就是大忌。</p>
<p>上述获取摘要的向前向后扩展一个步长长度的过程中，使<code>pos-prev_step</code>和0进行比较，如果<code>pos</code>小于步长，<code>pos-prev_step</code>就是负数，无符号数就变成很大的正数条件自然成立，就会导致<code>start</code>大于<code>end</code>。</p>
<h4 id="查找方式错误">查找方式错误</h4>
<p>使用<code>find</code>查找，是区分大小写的，而且我们在获取查询字段<code>query</code>的时候就已经将<code>query</code>全部转为小写，这样是无法查询到大写情况的。</p>
<pre><code class="language-cpp">// 忽略大小写查找，避免找不到的情况
auto iter = std::search(content.begin(), content.end(), word.begin(), word.end(),
                        [](char x, char y) {
                            return tolower(x) == tolower(y);
                        });
if (iter == content.end())
    return &quot;NONE&quot;;

int pos = iter - content.begin(); // 转化查找位置
</code></pre>
<h4 id="读取query方式错误">读取query方式错误</h4>
<p>如果单纯使用<code>cin</code>读取，会将query按空格拆成多个字段分别查找，不能一次返回。</p>
<pre><code class="language-cpp">getline(std::cin, query);
</code></pre>
<h4 id="权重计算错误">权重计算错误</h4>
<p>我们前面解析文档内容的函数有一定的错误，直接读取文档内容没有越过标题部分。导致标题部分的关键词，在标题中统计一次，在内容处又被统计一次。并不影响使用。</p>
<h4 id="汇总重复结果">汇总重复结果</h4>
<p> </p>
<h2 id="8-http服务的模块">8. http服务的模块</h2>
<h3 id="81-工具安装和使用">8.1 工具安装和使用</h3>
<h4 id="升级gcc">升级gcc</h4>
<p>我们项目的重点在于搜索功能的服务，所以网络服务就调用<code>cpp-httplib</code>完成。</p>
<p>https://gitee.com/welldonexing/cpp-httplib</p>
<p>使用 cpp-httplib 需要高版本的 gcc，单独安装gcc很麻烦，安装高版本的工具集可以<code>scl</code>安装。</p>
<pre><code class="language-shell">$ yum -y install centos-release-scl  # 1. 安装scl的yum源
$ yum -y install devtoolset-8        # 2. 安装工具集
$ scl enable devtoolset-8 bash       # 3. 启动工具集
$ source /opt/rh/devtoolset-8/enable # 4. 加载工具集（出错执行）
</code></pre>
<p>https://blog.csdn.net/weixin_43364556/article/details/108315111</p>
<p>注意我们安装的是工具集，没有卸载原有的gcc，工具集的启动仅本次会话有效。若想启动自动生效可以添加到<code>~/.bashrc</code>中。</p>
<h4 id="安装测试cpp-httplib">安装测试cpp-httplib</h4>
<blockquote>
<p>如果cpp-httplib的版本太高，而gcc版本没有很新的话，还是会发生编译报错货运行出错的问题。</p>
</blockquote>
<p>直接克隆仓库到本地，可以直接放到也可以创建软连接到项目目录下。</p>
<pre><code class="language-shell">$ git clone https://gitee.com/welldonexing/cpp-httplib ~/depts/
$ ln -s ~/depts/cpp-httplib/ ~/Project/BoostSearch/cpp-httplib
</code></pre>
<h5 id="基本使用">基本使用</h5>
<pre><code class="language-cpp">// 创建HTTP对象
httplib::Server svr;

// 设置web根目录
svr.set_base_dir(root_path);

// 注册GET方法
svr.Get(&quot;/hi&quot;, [](const httplib::Request&amp; req, httplib::Response&amp; rsp) {
    rsp.set_content(&quot;hello world，我是HTTP!&quot;, &quot;text/plain: charset=utf-8&quot;);
});

// 设置监听
svr.listen(&quot;0.0.0.0&quot;, 8080);
</code></pre>
<h3 id="82-http服务代码">8.2 http服务代码</h3>
<pre><code class="language-cpp">const std::string src_path = &quot;data/output/raw.bin&quot;;
const std::string root_path = &quot;./wwwroot&quot;;
int main() 
{
    Searcher* searcher = new Searcher();
    searcher-&gt;InitSearcher(src_path);
    // 创建HTTP对象
    httplib::Server svr;
    // 设置web根目录
    svr.set_base_dir(root_path);
    // 注册GET方法
    svr.Get(&quot;/hi&quot;, [](const httplib::Request&amp; req, httplib::Response&amp; rsp) {
        rsp.set_content(&quot;hello world，我是HTTP!&quot;, &quot;text/plain: charset=utf-8&quot;);
        });
    svr.Get(&quot;/s&quot;, [&amp;searcher](const httplib::Request&amp; req, httplib::Response&amp; rsp)
        {
            if (!req.has_param(&quot;word&quot;)) // 是否有参数
                rsp.set_content(&quot;输入有误，请重新输入!&quot;, &quot;text/plain: charset=utf-8&quot;);
            else
            {   // 提取参数
                std::string word = req.get_param_value(&quot;word&quot;);
                std::cout &lt;&lt; &quot;user query : &quot; &lt;&lt; word &lt;&lt; std::endl;
                std::string json_string;
                // 进行搜索
                searcher-&gt;Search(word, &amp;json_string);
                // 返回结果
                rsp.set_content(json_string, &quot;applicaton/json&quot;);
            }
        });
    // 设置监听
    svr.listen(&quot;0.0.0.0&quot;, 8080);

    delete searcher;
    return 0;
}
</code></pre>
<p> </p>
<h2 id="9-编写前端模块">9. 编写前端模块</h2>
<ul>
<li>html是超文本标记语言，由一个个标签构成网页的元素，属于网页的骨架。</li>
<li>css是网页美化语言，可以对网页元素内容进行精确控制。</li>
<li>javaScript可以对网页内容进行动态效果。</li>
</ul>
<h3 id="91-网页骨架">9.1 网页骨架</h3>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge&quot;&gt;
    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;
    &lt;title&gt;Document&lt;/title&gt;
    &lt;title&gt;Boost 搜索引擎&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;h1&gt;欢迎使用Boost库搜索引擎&lt;/h1&gt;
    &lt;div class=&quot;container&quot;&gt;
        &lt;div class=&quot;search&quot;&gt;
            &lt;input type=&quot;text&quot; value=&quot;请输入搜索内容&quot;&gt;
            &lt;button&gt;搜索一下&lt;/button&gt;
        &lt;/div&gt;
        &lt;div class=&quot;result&quot;&gt;
            &lt;div class=&quot;item&quot;&gt;
                &lt;a herf=&quot;#&quot;&gt;这是标题&lt;/a&gt;
                &lt;p&gt;这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要&lt;/p&gt;
                &lt;i&gt;https://gitee.com/welldonexing/cpp-httplib&lt;/i&gt;
            &lt;/div&gt;
            &lt;div class=&quot;item&quot;&gt;
                &lt;a herf=&quot;#&quot;&gt;这是标题&lt;/a&gt;
                &lt;p&gt;这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要&lt;/p&gt;
                &lt;i&gt;https://gitee.com/welldonexing/cpp-httplib&lt;/i&gt;
            &lt;/div&gt;
            &lt;div class=&quot;item&quot;&gt;
                &lt;a herf=&quot;#&quot;&gt;这是标题&lt;/a&gt;
                &lt;p&gt;这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要&lt;/p&gt;
                &lt;i&gt;https://gitee.com/welldonexing/cpp-httplib&lt;/i&gt;
            &lt;/div&gt;
            &lt;div class=&quot;item&quot;&gt;
                &lt;a herf=&quot;#&quot;&gt;这是标题&lt;/a&gt;
                &lt;p&gt;这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要这是摘要&lt;/p&gt;
                &lt;i&gt;https://gitee.com/welldonexing/cpp-httplib&lt;/i&gt;
            &lt;/div&gt;
        &lt;/div&gt;
    &lt;/div&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="92-网页美化">9.2 网页美化</h3>
<p>css设置样式的方式是：选择需要设置的标签，然后再制定该标签的样式。</p>
<pre><code class="language-css">&lt;style&gt;
* {
    margin: 0;
    padding: 0;
}

html,
body {
    height: 100%;
}
.container {
    width: 800px;
    margin: 0px auto;
    margin-top: 15px;
}
.container .search {
    width: 100%;
    height: 52px;
}
.container .search input {
    /* 设置左浮动 */
    float: left;
    width: 600px;
    height: 50px;
    border: 1px solid black;
    /* border: 1px solid #4A4C4E; */
    border-right: none;
    padding-left: 10px;
    color: #9295A2;
    font-size: 15px;
}
.container .search button {
    float: left;
    width: 140px;
    height: 52px;
    background-color: #63855C;
    color: white;
    font-size: 17px;
    font-style: normal;
    font-family: &quot;SF Pro SC&quot;, &quot;SF Pro Display&quot;, &quot;SF Pro Icons&quot;, &quot;PingFang SC&quot;, &quot;Helvetica Neue&quot;, Helvetica, Arial, sans-serif;
}

.container .result {
    width: 100%;
    font-family: &quot;SF Pro SC&quot;, &quot;SF Pro Display&quot;, &quot;SF Pro Icons&quot;, &quot;PingFang SC&quot;, &quot;Helvetica Neue&quot;, Helvetica, Arial, sans-serif;
}
.container .result .item {
    margin-top: 15px;
}
.container .result .item a {
    text-decoration: none;
    font-size: 20px;
    color: #63855C;
}
.container .result .item a:hover {
    text-decoration: underline;
}
.container .result .item p {
    font-size: 15px;
    margin-top: 5px;
    color: #1d1d1f;
}
.container .result .item i {		
    font-size: 13px;
    font-style: normal;
    color: #6e6e73;
}
&lt;/style&gt;
</code></pre>
<h3 id="93-前后端交互">9.3 前后端交互</h3>
<p>使用原生的JS要求较高，我们采用JQuery框架。JQuery中可以使用ajax来进行前后端交互，如发起http请求。</p>
<pre><code class="language-html">&lt;script&gt;
    function Search() {
    // alert(&quot;hello js!&quot;);
    // 1. 提取数据
    let query = $(&quot;.container .search input&quot;).val();
    console.log(&quot;query: &quot; + query);
    // 2. 发起http请求
    $.ajax({
        type: &quot;GET&quot;,
        url: &quot;/s?word=&quot; + query,
        success: function (data) {
            console.log(data);
            BuildHtml(data);
        }
    })
}
function BuildHtml(data) {
    // 获取result标签
    let tag_result = $(&quot;.container .result&quot;);
    // 清空历史结果
    tag_result.empty();

    for (let elem of data) {
        // console.log(elem.title);
        // console.log(elem.url);
        let tag_a = $(&quot;&lt;a&gt;&quot;, {
            text: elem.title,
            href: elem.url,
            target: &quot;_blank&quot;
        });
        let tag_p = $(&quot;&lt;p&gt;&quot;, {
            text: elem.desc
        });
        let tag_i = $(&quot;&lt;i&gt;&quot;, {
            text: elem.url
        });
        let tag_div = $(&quot;&lt;div&gt;&quot;, {
            class: &quot;item&quot;
        });

        tag_a.appendTo(tag_div);
        tag_p.appendTo(tag_div);
        tag_i.appendTo(tag_div);

        tag_div.appendTo(tag_result);
    }
}
&lt;/script&gt;
</code></pre>
<img src="https://img.gejiba.com/images/fb49a757d311fc7a4f255df1e50ad0a4.png" >
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[负载均衡式在线OJ]]></title>
        <id>https://yourfriendyo.github.io/post/fu-zai-jun-heng-shi-zai-xian-oj/</id>
        <link href="https://yourfriendyo.github.io/post/fu-zai-jun-heng-shi-zai-xian-oj/">
        </link>
        <updated>2023-01-14T18:17:29.000Z</updated>
        <content type="html"><![CDATA[<h1 id="负载均衡式oj">负载均衡式OJ</h1>
<h2 id="1-项目技术和开发环境">1. 项目技术和开发环境</h2>
<h3 id="项目技术">项目技术</h3>
<ul>
<li>C++ STL 标准库</li>
<li>Boost 库</li>
<li>cpp-httplib 第三方开源网络库</li>
<li>ctemplate 第三方开源前端网页渲染库</li>
<li>jsoncpp 第三方开源序列化、反序列化库</li>
<li>负载均衡设计</li>
<li>多进程、多线程</li>
<li>MySQL C Connect</li>
<li>html /css/js/jquery/ajax</li>
</ul>
<h3 id="开发环境">开发环境</h3>
<ul>
<li>centos 7 服务器</li>
<li>vim / gcc(g++) / makefile</li>
</ul>
<p> </p>
<h2 id="2-结构设计和实现思路">2. 结构设计和实现思路</h2>
<img src="https://img.gejiba.com/images/e7cae9c024d3a18ec9a3f42bad44df1b.png" style="zoom:50%;" >
<p>我们的项目核心是如下三个模块：</p>
<table>
<thead>
<tr>
<th>目录</th>
<th>介绍</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>comm</code></td>
<td>公共模块，存放公用的代码如一些工具类</td>
</tr>
<tr>
<td><code>compile_server</code></td>
<td>编译模块，编译运行远端提交的代码</td>
</tr>
<tr>
<td><code>oj_server</code></td>
<td>服务模块，提供题目列表、题目查看、题目编写，实现反向代理负载均衡的功能</td>
</tr>
</tbody>
</table>
<blockquote>
<p>在线判题方面我们只实现类似牛客、力扣等网站的题目列表和在线编程的功能。</p>
</blockquote>
<p>项目目录结构大致如下：</p>
<pre><code class="language-shell">OnlineJudge/
├── comm/
│   ├── httplib.h -&gt; ../depts/cpp-httplib/httplib.h
│   ├── log.hpp
│   └── util.hpp
├── compile_server/
│   ├── compile_server.cc
│   ├── compile_run.hpp
│   ├── compiler.hpp
│   ├── runner.hpp
│   ├── Makefile
│   └── temp/
├── oj_server/
│   ├── oj_server.cc
│   ├── oj_model.hpp
│   ├── oj_controller.hpp
│   ├── oj_view.hpp
│   └── Makefile
└── Makefile
</code></pre>
<p> </p>
<h3 id="21-日志模块">2.1 日志模块</h3>
<pre><code class="language-cpp">#pragma once
#include &lt;iostream&gt;
#include &lt;string&gt;
#include &quot;util.hpp&quot;

namespace NS_Log
{
    using namespace NS_Util;

    // log level
    enum
    {
        INFO,
        DEBUG,
        WARNING,
        ERROR,
        FATAL
    };

    // LOG() &lt;&lt; &quot;xxxxxx&quot;;
	std::ostream&amp; Log(const std::string&amp; level, const std::string&amp; file_name, int line)
    {
        std::string message = &quot;[&quot; + level + &quot;]&quot;;         // 添加日志等级

        message += &quot;[&quot; + file_name + &quot;]&quot;;                // 获取报错文件
        message += &quot;[&quot; + std::to_string(line) + &quot;]&quot;;     // 获取报错行号
        message += &quot;[&quot; + TimeUtil::GetTimeStamp() + &quot;]&quot;; // 获取日志时间

        std::cout &lt;&lt; message; // 存入缓冲区，不刷新待填充报错信息
        return std::cout;
    }

#define LOG(level) Log(#level, __FILE__, __LINE__)
}
</code></pre>
<p> </p>
<h3 id="22-编译运行模块">2.2 编译运行模块</h3>
<h4 id="编译模块">编译模块</h4>
<img src="https://img.gejiba.com/images/6d7b7ff8f8caee6ca1777baeabd8e7d2.png" style="zoom:50%;" >
<pre><code class="language-cpp">// ret: 编译是否成功
// file_name: 编译文件名
// ./temp/123.cpp
// ./temp/123.exe
// ./temp/123.err
static bool Compile(const std::string&amp; file_name)
{
    pid_t pid = fork();

    if (pid == 0) /* child */
    {
        int fd = open(PathUtil::Err(file_name).c_str(), O_CREAT | O_WRONLY, 644);
        if (fd &lt; 0)
        {
            LOG(WARNING) &lt;&lt; &quot;cannot open err file, maybe not existed\n&quot;;
            exit(1);
        }

        dup2(err_fd, 2); // 将报错信息重定向至.stderr文件中

        //g++ -o 123.exe 123.cpp -std=c++11
        execlp(
            &quot;g++&quot;,
            &quot;-o&quot;,
            PathUtil::Exe(file_name).c_str(),
            PathUtil::Src(file_name).c_str(),
            &quot;-std=c++11&quot;,
            nullptr
        );

        LOG(ERROR) &lt;&lt; &quot;compile(g++) file failed, exec's arguments were wrong\n&quot;;
        exit(2);

    }
    else if (pid &gt; 0) /* parent */
    {
        waitpid(pid, nullptr, 0);

        if (FileUtil::IsFileExists(PathUtil::Exe(file_name))) // 编译成功
        {
            LOG(INFO) &lt;&lt; &quot;compile(g++) success, get &quot; &lt;&lt; PathUtil::Exe(file_name);
            return true;
        }
    }
    else /* pid &lt; 0 error */
    {
        LOG(ERROR) &lt;&lt; &quot;child proccess create failed\n&quot;;
        return false;
    }
}
</code></pre>
<h4 id="运行模块">运行模块</h4>
<p>运行功能由<code>runner.hpp</code>提供。</p>
<p>运行模块不需要考虑代码运行的结果是否正确，只关心程序是否正常退出。程序的退出信息交给上层模块处理。</p>
<p>其次我们要控制程序的输入输出，</p>
<ul>
<li>标准输入：不作处理</li>
<li>标准输出：一般是程序运行的结果</li>
<li>标准错误：运行时错误信息</li>
</ul>
<pre><code class="language-cpp">static int Run(const std::string&amp; file_name)
{
    std::string exe_file = PathUtil::Exe(file_name);
    int sin_fd = FileUtil::OpenFile(PathUtil::Stdin(file_name));
    int sout_fd = FileUtil::OpenFile(PathUtil::Stdout(file_name));
    int serr_fd = FileUtil::OpenFile(PathUtil::Stderr(file_name));

    if (sin_fd == 0 || sin_fd == 0 || serr_fd == 0)
        exit(3);

    pid_t pid = fork();
    if (pid == 0) /* child */
    {
        //重定向标准输出输入错误
        dup2(sin_fd, 0);
        dup2(sout_fd, 1);
        dup2(serr_fd, 2);
        SetProcLimit(cpu_limit, mem_limit); // 资源约束

        // ./tmp/code.out
        execl(exe_file.c_str(), exe_file.c_str());

        exit(-2);
    }
    else if (pid &gt; 0) /* parent */
    {
        int status = 0;
        waitpid(pid, &amp;status, 0);
        return status &amp; 0x7F; // 返回程序的退出信息，具体情况交给oj模块处理
    }
    else /* error */
    {
        close(sin_fd);
        close(sout_fd);
        close(serr_fd);
    }
}
</code></pre>
<h5 id="限制时空复杂度">限制时空复杂度</h5>
<p>通过<code>setrlimit()</code>限制程序的资源的占用大小。如CPU时间，占用空间大小等。分别通过发出<code>24)SIGCPU</code>和<code>6)SIGABRT</code>信号终止程序。</p>
<p>故我们可以在运行子进程之前设置资源约束，在子进程运行结束后也可以通过父进程等待的返回值，确定子进程是否是异常退出以及是否收到第几位信号。</p>
<pre><code class="language-cpp">// 设置子进程运行占用资源的大小
static void SetProcLimit(int cpu_limit, int mem_limit)
{
    struct rlimit cpu_rl;
    cpu_rl.rlim_max = RLIM_INFINITY;
    cpu_rl.rlim_cur = cpu_limit;
    setrlimit(RLIMIT_CPU, &amp;cpu_rl);

    struct rlimit mem_rl;
    mem_rl.rlim_max = RLIM_INFINITY;
    mem_rl.rlim_cur = mem_limit * 1024; // 转化成KB
    setrlimit(RLIMIT_AS, &amp;mem_rl);
}
</code></pre>
<h4 id="编译运行模块">编译运行模块</h4>
<img src="https://img.gejiba.com/images/9b8e05359f869ada83a088f297fb4b95.png" style="zoom:50%;"  >
<p><code>compile_run.hpp</code>实现编译和运行功能。</p>
<p>要能够适配用户需求，定制通信协议字段，正确调用编译和运行模块。</p>
<pre><code class="language-cpp">/***************************************************************
 * 输入json串：
 *   code    : 用户提交代码
 *   input   : 用户提交代码的标准输入内容，不作处理以待扩展
 *   cpu_lim : 时间复杂度
 *   mem_lim : 空间复杂度
 * {&quot;code&quot;: &quot;#include...&quot;, &quot;input&quot;: &quot;&quot;, &quot;cpu_lim&quot;: &quot;1&quot;, &quot;mem_lim&quot;: &quot;10240&quot;}
 *
 * 输出json串：
 *   必填：
 *   status  : 状态码
 *   reason  : 结果原因
 *   选填：
 *   stdout  : 用户提交代码的运行正确结果
 *   stderr  : 用户提交代码的运行错误结果
 * {&quot;status&quot;: &quot;0&quot;, &quot;reason&quot;: &quot;&quot;, &quot;stdout&quot;: &quot;&quot;, &quot;stdin&quot;: &quot;&quot;}
***************************************************************/
static void Start(const std::string&amp; in_json, std::string* out_json)
{
    // 反序列化
    Json::Value in_value;
    Json::Reader reader;
    reader.parse(in_json, in_value); // 解析in_json

    std::string code = in_value[&quot;code&quot;].asString();
    std::string input = in_value[&quot;input&quot;].asString();
    int cpu_limit = in_value[&quot;cpu_lim&quot;].asInt();
    int mem_limit = in_value[&quot;mem_lim&quot;].asInt();

    int status_code = 0;
    int ret_code = 0;
    std::string file_name;

    if (code.size() == 0)
    {
        status_code = -1; // 代码为空
        goto ERROR;
    }
    // 生成源文件并写入
    file_name = FileUtil::UniqueFileName();
    if (!FileUtil::WriteFile(PathUtil::Src(file_name), code))
    {
        status_code = -2; // 文件写入错误
        goto ERROR;
    }
    // 编译源文件
    if (!Compiler::Compile(file_name))
    {
        status_code = -3; // 代码编译错误
        goto ERROR;
    }
    // 运行可执行文件
    ret_code = Runner::Run(file_name, cpu_limit, mem_limit);
    if (ret_code &lt; 0) // 内部错误
    {
        status_code = -2;
        goto ERROR;
    }
    else // ret_code &gt;= 0 程序异常或正常退出
    {
        status_code = ret_code;
        goto ERROR;
    }
    ERROR:
    Json::Value out_value;
    out_value[&quot;status&quot;] = status_code;
    out_value[&quot;reason&quot;] = StatusUtil::CodeToDesc(status_code, file_name);
    if (status_code == 0)
    {
        out_value[&quot;stdout&quot;] = FileUtil::ReadFile(PathUtil::Stdout(file_name));
        out_value[&quot;stderr&quot;] = FileUtil::ReadFile(PathUtil::Stderr(file_name));
    }
    //序列化
    Json::StyledWriter writer;
    *out_json = writer.write(out_value);
    
    FileUtil::DeleteTempFile(file_name); // 清理临时文件
}
</code></pre>
<h4 id="服务模块">服务模块</h4>
<p>编译服务随时可能被多人请求，必须保证上传调源文件名唯一。下面代码仅用来测试功能是否正确。</p>
<pre><code class="language-cpp">//客户端通过HTTP协议向编译服务上传一个json串，编译模块发送回一个json串
int main()
{
    std::string in_json;
    std::string out_json;

    Json::Value in_value;
    in_value[&quot;code&quot;] = R&quot;(#include &lt;iostream&gt;
int main()
{
    std::cout &lt;&lt; &quot;hello world&quot; &lt;&lt; std::endl;
    // int* p = new int[1024*1024*1024];
    // while (1);
    // int a = 1 / 0;
    // aaa
    return 0;
})&quot;;
    in_value[&quot;input&quot;] = &quot;&quot;;
    in_value[&quot;cpu_lim&quot;] = 1;
    in_value[&quot;mem_lim&quot;] = 1024 * 30;

    Json::StyledWriter writer;
    in_json = writer.write(in_value);
    std::cout &lt;&lt; in_json &lt;&lt; std::endl;

    CompileAndRun::Start(in_json, &amp;out_json);

    std::cout &lt;&lt; out_json &lt;&lt; std::endl;

    return 0;
}
</code></pre>
<p>我们使用<code>cpp-httplib</code>来将编译服务模块打包成网络服务，<code>cpp-httplib</code>要求<code>gcc/g++</code>版本必须高于<code>7</code>。我们使用<code>scl</code>工具集安装：</p>
<pre><code class="language-shell">$ sudo yum install centos-release-scl scl-utils-build       # 安装scl yum源
$ sudo yum install -y devtoolset-9-gcc devtoolset-9-gcc-c++ # 安装scl gcc版本工具集
$ ls /opt/rh/                                               # 查看安装工具集
$ scl enable devtoolset-9 bash                              # 启动工具集
$ gcc -v         
</code></pre>
<pre><code class="language-cpp">int main()
{
    Server svr;

    // 用来基本测试
    svr.Get(&quot;/hello&quot;, [](const Request&amp; req, Response&amp; rsp) {
            rsp.set_content(&quot;hello httplib, 你好网络库&quot;, &quot;text/pain; charset=utf-8&quot;);
        }
    );

    // 提供编译运行服务
    svr.Post(&quot;/compile_and_run&quot;, [](const Request&amp; req, Response&amp; rsp)
        {
            std::string in_json = req.body;
            std::string out_json;
            if (!in_json.empty())
            {
                CompileAndRun::Start(in_json, &amp;out_json);
                rsp.set_content(out_json, &quot;application/json; charset=utf-8&quot;);
            }
        }
    );

    svr.listen(&quot;0.0.0.0&quot;, 8080); // 启动http服务
    return 0;
}
</code></pre>
<p>如上代码就是我们的网络编译服务，我们采用 Posman 进行测试。</p>
<img src="https://img.gejiba.com/images/efd2a8002767a76db63ae126162d6601.png" style="zoom:50%;" >
<p>至此，我们的编译运行服务模块就完成了。</p>
<blockquote>
<p>为什么将编译运行模块独立成一个服务呢？</p>
</blockquote>
<p>编译运行服务比较耗时耗资源，危险系数比较高，独立出来便于进行分布部署在多台主机。</p>
<p> </p>
<h3 id="23-oj服务模块">2.3 OJ服务模块</h3>
<p>OJ服务本质就是建立一个小型的在线判题网站。我们只提供最基本的网站功能：</p>
<ol>
<li>首页获取：使用题库列表页面充当首页；</li>
<li>代码编辑：</li>
<li>代码提交：对应编译和运行模块。</li>
</ol>
<p>我们采用MVC结构来设计OJ服务模块，何为MVC呢？</p>
<table>
<thead>
<tr>
<th>概念</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>M</code></td>
<td><code>Model</code>，进行数据交互的模块，比如对题库进行增删改查。</td>
</tr>
<tr>
<td><code>V</code></td>
<td><code>View</code>，拿到数据后，进行构建和渲染网页。</td>
</tr>
<tr>
<td><code>C</code></td>
<td><code>Control</code>，控制数据交互等，就是我们的核心业务逻辑。</td>
</tr>
</tbody>
</table>
<h4 id="服务路由模块">服务路由模块</h4>
<p>用户请求的服务不同，我们就要进行不同的工作，所以我们首先要实现服务路由的功能。</p>
<pre><code class="language-cpp">// 服务路由功能
Server svr;

/* 获取题库列表 */
svr.Get(&quot;/problem_set&quot;, [](const Request&amp; req, Response&amp; rsp) {
    rsp.set_content(&quot;这是所有题目的列表&quot;, &quot;text/plain; charset=utf8&quot;);
});
/* 根据题号获取题目内容 */
// /problems/10 正则表达式匹配
svr.Get(R&quot;(/problems/(\d+))&quot;, [](const Request&amp; req, Response&amp; rsp) {
    std::string problem_number = req.matches[1];
    rsp.set_content(&quot;这是指定的一道题，题号：&quot; + problem_number, &quot;text/plain; charset=utf8&quot;);
});
/* 提交代码，使用编译运行服务 */
svr.Get(R&quot;(/judge/(\d+))&quot;, [](const Request&amp; req, Response&amp; rsp) {
    std::string problem_number = req.matches[1];
    rsp.set_content(&quot;这是该题的判题，题号：&quot; + problem_number, &quot;text/plain; charset=utf8&quot;);
});

svr.set_base_dir(&quot;./wwwroot&quot;);
svr.listen(&quot;0.0.0.0&quot;, 8080);
</code></pre>
<h4 id="题目题库设计">题目题库设计</h4>
<p>题目具有如下属性：题目的编号，题目的标题，题目的难度，题目的题干，题目的时间空间要求，测试用例等等。</p>
<p>我们建立两张表，一张是题库列表，第二张表存放题目的描述、题目的预设置代码<code>header.cpp</code>和测试用例代码<code>tail.cpp</code>等等。</p>
<pre><code class="language-cpp">// header.cpp
#include &lt;iostream&gt;
#include &lt;string&gt;
#include &lt;vector&gt;
#include &lt;map&gt;
#include &lt;algorithm&gt;
using namespace std;

class Solution {
public:
    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) {
        // coding here...

    }
};
</code></pre>
<pre><code class="language-cpp">// tail.cpp
#ifndef COMPILER_ONLINE
#include &quot;header.cpp&quot;
#endif

void Test1()
{
    vector&lt;int&gt; nums = { 2,7,11,15 };
    vector&lt;int&gt; ret = Solution().twoSum(nums, 9);
    if (find(ret.begin(), ret.end(), 0) != ret.end() &amp;&amp;
        find(ret.begin(), ret.end(), 1) != ret.end())
    {
        std::cout &lt;&lt; &quot;通过测试用例1&quot; &lt;&lt; std::endl;
    }
    else
    {
        std::cout &lt;&lt; &quot;测试用例1失败&quot; &lt;&lt; std::endl;
    }
}

void Test2()
{
    vector&lt;int&gt; nums = { 3,2,4 };
    vector&lt;int&gt; ret = Solution().twoSum(nums, 6);
    if (find(ret.begin(), ret.end(), 2) != ret.end() &amp;&amp;
        find(ret.begin(), ret.end(), 4) != ret.end())
    {
        std::cout &lt;&lt; &quot;通过测试用例2&quot; &lt;&lt; std::endl;
    }
    else
    {
        std::cout &lt;&lt; &quot;测试用例2失败&quot; &lt;&lt; std::endl;
    }
}

void Test3()
{
    vector&lt;int&gt; nums = { 3,3 };
    vector&lt;int&gt; ret = Solution().twoSum(nums, 6);
    if (find(ret.begin(), ret.end(), 0) != ret.end() &amp;&amp;
        find(ret.begin(), ret.end(), 1) != ret.end())
    {
        std::cout &lt;&lt; &quot;通过测试用例1&quot; &lt;&lt; std::endl;
    }
    else
    {
        std::cout &lt;&lt; &quot;测试用例1失败&quot; &lt;&lt; std::endl;
    }
}

int main()
{
    Test1();
    Test2();
    Test3();

    return 0;
}
</code></pre>
<p>合并<code>header.cpp</code>和<code>tail.cpp</code>，就是提交到后台编译运行服务的最终代码。</p>
<h4 id="model模块">Model模块</h4>
<p><code>Model</code>模块负责数据处理功能，大致框架如下：</p>
<pre><code class="language-cpp">struct Problem
{
    std::string _number;     // 题目编号
    std::string _title;      // 题目标题
    std::string _desc;       // 题目描述
    std::string _difficulty; // 题目难度 简单 中等 困难

    int _cpu_limit;          // 题目的时间要求
    int _mem_limit;          // 题目的空间要求

    std::string _header;     // 题目的预设代码
    std::string _tail;       // 题目的测试用例
};
const std::string problem_list = &quot;./problem_set/problems.list&quot;;

class Model
{
    public:
    Model();

    // 加载题库列表文件和所有题目文件
    bool LoadProblemList(const std::string&amp; question_list);
    void GetAllProblems(std::vector&lt;Problem&gt;* out);
    void GetOneProblems(const std::string&amp; problem_number, Problem* problem);
    
    ~Model();

    private:
    std::unordered_map&lt;std::string, Problem&gt; _problem_set; // 题号和题目内容的映射
};
</code></pre>
<h4 id="control模块">Control模块</h4>
<p><code>Control</code>模块为控制器，用来调用控制<code>Model</code>和<code>View</code>模块的。</p>
<pre><code class="language-cpp">class Controller
{
    private:
    Model _model;
    View _view;

    public:
    Controller();
    // 构建题库网页
    bool GetAllQuestionsHtml(std::string* html);
    // 构建题目编辑网页
    bool GetOneQuestionHtml(const std::string&amp; number, std::string* html);
    ~Controller();
};
</code></pre>
<p>而它本身又是被<code>oj_server</code>所调用。用来调用我们对应的一些功能。</p>
<pre><code class="language-cpp">Server svr;
Controller ctrl;

/* 获取题库列表 */
svr.Get(&quot;/all_questions&quot;, [&amp;ctrl](const Request&amp; req, Response&amp; rsp) {
    std::string html;
    ctrl.GetAllQuestionsHtml(&amp;html);
    rsp.set_content(html, &quot;text/html; charset=utf-8&quot;);
});

/* 根据题号获取题目内容 */
// /questions/10 正则表达式匹配
svr.Get(R&quot;(/question/(\d+))&quot;, [&amp;ctrl](const Request&amp; req, Response&amp; rsp) {
    std::string question_number = req.matches[1];
    std::string html;
    ctrl.GetOneQuestionHtml(question_number, &amp;html);
    rsp.set_content(html, &quot;text/html; charset=utf-8&quot;);
});

/* 提交代码，使用编译运行服务 */
svr.Get(R&quot;(/judge/(\d+))&quot;, [&amp;ctrl](const Request&amp; req, Response&amp; rsp){
    //...
});
</code></pre>
<p><a href="#judge">跳转到<code>judge</code>编译运行服务功能实现处</a></p>
<blockquote>
<p>judge编译运行服务功能需要先实现View模块，不影响实现思路，我们将该模块的实现放到下面。</p>
</blockquote>
<h4 id="view模块">View模块</h4>
<p><code>View</code>模块就是用来构建一些网页的，<code>Control</code>模块中构建网页的方法就是调用这里的。</p>
<p>渲染网页我们选择用谷歌的<code>ctemplate</code>开源库。<code>ctamplate</code>需要我们提供<code>json</code>串和<code>html</code>网页骨架。</p>
<ul>
<li><code>json</code>串就是保存数据的数据字典，也就是一个一个的<code>kv</code>结构。</li>
<li>原始网页内容包含<code>json</code>串中的<code>key</code>值，ctemplate所作的渲染就是将<code>key</code>值对应的<code>value</code>导入到网页中。</li>
</ul>
<img src="https://img.gejiba.com/images/cc5dc77678456769eb40041cf8c10949.png" style="zoom:50%;"  >
<pre><code class="language-cpp">class View
{
    public:
    View()
    {}
    ~View()
    {}
    // 题库列表只需显示题目编号、题目标题、题目难度
    void ExpandAllQuestionsHtml(const std::vector&lt;Question&gt; questions, std::string* html)
    {
        std::string src_html = template_path + &quot;all_questions.html&quot;; // 路径
        // 形成数据字典
        TemplateDictionary root(&quot;all_questions&quot;);
        for (const auto&amp; q : questions)
        {
            TemplateDictionary* sub =root.AddSectionDictionary(&quot;questions_list&quot;);
            sub-&gt;SetValue(&quot;number&quot;, q._number);
            sub-&gt;SetValue(&quot;title&quot;, q._title);
            sub-&gt;SetValue(&quot;difficulty&quot;, q._difficulty);
        }

        // 获取被渲染的网页
        Template* tpl = Template::GetTemplate(src_html, DO_NOT_STRIP);

        tpl-&gt;Expand(html, &amp;root); // 开始渲染
    }

    void ExpandOneQuestionHtml(Question question, std::string* html)
    {
        // 路径
        std::string src_html = template_path + &quot;one_question.html&quot;;
        // 形成数据字典
        TemplateDictionary root(&quot;one_question&quot;);
        root.SetValue(&quot;number&quot;, question._number);
        root.SetValue(&quot;title&quot;, question._title);
        root.SetValue(&quot;difficulty&quot;, question._difficulty);
        root.SetValue(&quot;desc&quot;, question._desc);
        root.SetValue(&quot;header_code&quot;, question._header);

        // 获取被渲染的网页
        Template* tpl = Template::GetTemplate(src_html, DO_NOT_STRIP);
        //
        tpl-&gt;Expand(html, &amp;root);
    }
};
</code></pre>
<p>待渲染的网页如下所示：</p>
<pre><code class="language-html">&lt;!-- all_question --&gt;
&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;title&gt;Online Judge - question list&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;table&gt;
        &lt;tr&gt;
            &lt;th&gt;编号&lt;/th&gt;
            &lt;th&gt;标题&lt;/th&gt;
            &lt;th&gt;难度&lt;/th&gt;
        &lt;/tr&gt;
        {{#questions_list}}
        &lt;tr&gt;
            &lt;td&gt;{{number}}&lt;/td&gt;
            &lt;!-- 题目跳转链接 --&gt;
            &lt;td&gt;&lt;a href=&quot;/question/{{number}}&quot;&gt;{{title}}&lt;/a&gt;&lt;/td&gt;
            &lt;td&gt;{{difficulty}}&lt;/td&gt;

        &lt;/tr&gt;
        {{/questions_list}}
    &lt;/table&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<pre><code class="language-html">&lt;!-- one_question --&gt;
&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;title&gt;{{number}}.{{title}}&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;h4&gt;{{number}}.{{title}}.{{difficulty}}&lt;/h4&gt;
    &lt;p&gt;{{desc}}&lt;/p&gt;
    &lt;!-- 代码编辑区 --&gt;
    &lt;textarea name=&quot;&quot; id=&quot;&quot; cols=&quot;30&quot; rows=&quot;10&quot;&gt;{{header_code}}&lt;/textarea&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>放到指定目录下，由<code>View</code>模块调用，最后后由<code>oj_server</code>中的<code>Control</code>模块调用。</p>
<p> </p>
<h4 id="负载均衡功能">负载均衡功能</h4>
<p>负载均衡首先我们要有关于服务主机的配置类，负载均衡功能类。此外，我们可以在目录下新建<code>server.conf</code>作为服务主机的配置文件。</p>
<pre><code class="language-cpp">// 编译服务主机
struct Server
{
    std::string _ip;
    int _port;
    std::atomic&lt;uint64_t&gt; _load_factor; // 服务主机的负载情况

    Server() : _ip(&quot;&quot;), _port(-1), _load_factor(0)
    {}
    ~Server()
    {}

    void IncFactor() {
        _load_factor++;
    }

    void DecFactor() {
        _load_factor--;
    }

    uint64_t GetFactor() {
        return _load_factor;
    }
};

// 负载均衡模块
class LoadBalance
{
    public:
    LoadBalance() 
    {
        assert(LoadConf());
        LOG(INFO) &lt;&lt; &quot;load severs conf success\n&quot;;
    }
    ~LoadBalance()
    {}

    public:
    bool LoadConf();

    // 智能选择服务主机
    bool SmartSelection(int* id, Server** server)
    {
        std::lock_guard&lt;std::mutex&gt; lck(_mtx); // 维护负载选择的线程安全

        // 轮询检测
        int online_num = _online_svrs.size(); // 获取在线主机的个数
        if (online_num == 0)
        {
            LOG(FATAL) &lt;&lt; &quot;all server machines are offlined now, please check them\n&quot;;
            return false;
        }

        // 挑选负载最小的服务主机
        uint64_t min_load = _servers[_online_svrs[0]].GetFactor();
        *id = _online_svrs[0];
        *server = &amp;_servers[_online_svrs[0]];

        for (int i = 0; i &lt; online_num; i++)
        {
            uint64_t cur_load = _servers[_online_svrs[i]].GetFactor();
            if (min_load &gt; cur_load)
            {
                min_load = cur_load;
                *id = _online_svrs[i]; // 获取该主机id
                *server = &amp;_servers[_online_svrs[i]]; //获取该主机类地址
            }
        }
        return true;
    }
    
    //...
};
</code></pre>
<h4 id="a-idjudgejudge功能实现a"><a id="judge">Judge功能实现</a></h4>
<p>Control 模块 Judge 功能实现的是向用户提供包装后的编译运行功能。</p>
<ol>
<li>根据题号获取该题目的信息；</li>
<li>反序列化in_json，获取用户代码汇总用户代码和测试用例，形成compile_string；</li>
<li>负载均衡选择好后台编译服务主机，并作客户端对该主机发起http请求；</li>
<li>获取编译服务返回的json串，在返回给上层。</li>
</ol>
<pre><code class="language-cpp">// in_json
//   &quot;code&quot;: &quot;#incude...&quot;,
//   &quot;input&quot;: &quot;...&quot;,
void Judge(const std::string&amp; number, const std::string&amp; in_json, std::string* out_json)
{

    Question question;
    _model.GetOneQuestion(number, &amp;question); // 根据编号获取题目细节

    // 反序列化in_json，获取用户代码
    Json::Value in_value;
    Json::Reader reader;
    reader.parse(in_json, in_value);

    // 汇总用户代码和测试用例，形成compile_string
    Json::Value out_value;
    out_value[&quot;input&quot;] = in_value[&quot;input&quot;].asString();
    out_value[&quot;code&quot;] = in_value[&quot;code&quot;].asString() + question._tail;
    out_value[&quot;cpu_lim&quot;] = question._cpu_limit;
    out_value[&quot;mem_lim&quot;] = question._mem_limit;
    Json::StyledWriter writer;
    std::string compile_string = writer.write(out_value);

    // 负载均衡选择主机
    while (true) // 一直选择到可用主机，否则就是全部挂掉
    {
        int id = 0;
        Server* server = nullptr;
        if (!_load_balance.SmartSelection(&amp;id, &amp;server))
            break;

        LOG(INFO) &lt;&lt; &quot;select lowset load_factor server success, id :&quot;
            &lt;&lt; id &lt;&lt; &quot; ip: &quot; &lt;&lt; server-&gt;_ip &lt;&lt; &quot; port: &quot; &lt;&lt; server-&gt;_port &lt;&lt; &quot;\n&quot;;

        // 作客户端对后台编译运行服务发起http请求
        httplib::Client cli(server-&gt;_ip, server-&gt;_port);
        server-&gt;IncFactor();

        if (auto res = cli.Post(
            &quot;/compile_and_run&quot;,
            compile_string, &quot;application/json;charset=utf-8&quot;))
        {
            if (res-&gt;status == 200) // http状态码为200
            {
                *out_json = res-&gt;body; // 获取编译服务返回json串
                server-&gt;DecFactor();

                LOG(INFO) &lt;&lt; &quot;judge mudel resquest compile_and_run service success\n&quot;;
                break;
            }
            server-&gt;DecFactor();
        }
        else // 请求失败
        {
            LOG(ERROR) &lt;&lt; &quot;request compile_and_run server failed, id: &quot;
                &lt;&lt; id &lt;&lt; &quot; ip: &quot; &lt;&lt; server-&gt;_ip &lt;&lt; &quot; port: &quot; &lt;&lt; server-&gt;_port
                &lt;&lt; &quot;, maybe this server is offlined\n&quot;;

            _load_balance.OfflineServer(id); // 记录离线主机id
            _load_balance.ShowOnlineSvrList(); // for debug
        }
    }
}
</code></pre>
<p> </p>
<h2 id="3-前端页面和数据库化">3. 前端页面和数据库化</h2>
<p>我们作为后端开发，只用三个前端网页即可，作用就是感受整体项目的一致性。</p>
<table>
<thead>
<tr>
<th>网页</th>
<th>功能</th>
</tr>
</thead>
<tbody>
<tr>
<td>OJ首页</td>
<td>非常简单，只有一个跳转到题库列表的链接功能。</td>
</tr>
<tr>
<td>题库列表</td>
<td>利用表格的形式展示题库中所有的题目信息。</td>
</tr>
<tr>
<td>题目编写</td>
<td>单个指定题目的详情展示和代码编写页面，具有代码提交功能。</td>
</tr>
</tbody>
</table>
<p>对于前端，了解即可。相对重点关注前后端交互的部分。</p>
<h3 id="31-oj首页">3.1 OJ首页</h3>
<pre><code class="language-html">&lt;!-- OJ首页 --&gt;
&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;
&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;title&gt;OJ主页&lt;/title&gt;
    &lt;style&gt;
        * {
            margin: 0px;
            padding: 0px;
        }
        html,
        body {
            width: 100%;
            height: 100%;
        }
        .container .navbar {
            width: 100%;
            height: 50px;
            background-color: #343333;
            overflow: hidden;
        }

        .container .navbar a {
            display: inline-block;
            width: 40px;
            color: #f5f5f7;
            font-size: 15px;
            font-family: &quot;PingFang SC&quot;;
            line-height: 40px;
            text-decoration: none;
            margin-left: 10px;
        }

        .container .content {
            display: block;
            width: 800px;
            margin: 0px auto;
            text-align: center;
            text-decoration: none;
            font-size: larger;
            font-family: &quot;PingFang SC&quot;;
            line-height: 50px;
            margin-top: 200px;
        }

        .container .content p {
            text-decoration: none;
            font-size: 17px;
        }

        .container .content a {
            text-decoration: none;
            font-size: 17px;
        }
    &lt;/style&gt;
&lt;/head&gt;

&lt;body&gt;
    &lt;div class=&quot;container&quot;&gt;

        &lt;!-- 导航栏 --&gt;
        &lt;div class=&quot;navbar&quot;&gt;
            &lt;a href=&quot;#&quot;&gt;首页&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;题库&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;学习&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;竞赛&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;讨论&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;求职&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;登录&lt;/a&gt;
        &lt;/div&gt;

        &lt;!-- 网页内容 --&gt;
        &lt;div class=&quot;content&quot;&gt;
            &lt;h2&gt;欢迎来到我的OJ在线编程平台&lt;/h2&gt;
            &lt;p&gt;由开发者yyo独立开发的一款在线判题平台&lt;/p&gt;
            &lt;a href=&quot;/all_questions&quot;&gt;点我开始编写题目啦&lt;/a&gt;
        &lt;/div&gt;
    &lt;/div&gt;
&lt;/body&gt;

&lt;/html&gt;
</code></pre>
<h3 id="32-题库列表">3.2 题库列表</h3>
<img src="https://img.gejiba.com/images/9cb21fd09376141b27d5ed3567a33cdd.png" a style="zoom:50%;" >
<pre><code class="language-html">&lt;!-- 题库列表 --&gt;
&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;

&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;title&gt;Online Judge - question list&lt;/title&gt;
    &lt;style&gt;
        * {
            margin: 0px;
            padding: 0px;
        }

        html,
        body {
            width: 100%;
            height: 100%;
        }

        .container .navbar {
            width: 100%;
            height: 50px;
            background-color: #343333;
            overflow: hidden;
        }

        .container .navbar a {
            display: inline-block;
            width: 40px;
            color: #f5f5f7;
            font-size: 15px;
            font-family: &quot;PingFang SC&quot;;
            line-height: 40px;
            text-decoration: none;
            margin-left: 10px;
        }

        .container .questions_list {
            padding-top: 30px;
            width: 800px;
            height: 100%;
            margin: 0px auto;
            font-family: &quot;PingFang SC&quot;;
            text-align: center;
        }

        /* .container .questions_list h1 {} */

        .container .questions_list table {
            padding-top: 20px;
            width: 100%;
            font-size: large;
            background-color: rgb(250, 250, 250);
        }

        .container .questions_list table .item {
            width: 100px;
            height: 30px;
            padding-top: 10px;
            font-size: large;
        }

        .container .questions_list table .item a {
            text-decoration: none;
            color: black;

        }

        .container .questions_list table .item a:hover {
            color: #3478F6;
        }

        .footer {
            margin-top: 150px;
            width: 100%;
            height: 50px;
            line-height: 50px;
            text-align: center;
            color: #9d9d9d;
            background-color: #fbfbfb;
            font-family: &quot;PingFang SC&quot;;
        }
    &lt;/style&gt;
&lt;/head&gt;

&lt;body&gt;
    &lt;div class=&quot;container&quot;&gt;

        &lt;!-- 导航栏 --&gt;
        &lt;div class=&quot;navbar&quot;&gt;
            &lt;a href=&quot;/&quot;&gt;首页&lt;/a&gt;
            &lt;a href=&quot;/all_questions&quot;&gt;题库&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;学习&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;竞赛&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;讨论&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;求职&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;登录&lt;/a&gt;
        &lt;/div&gt;

        &lt;div class=&quot;questions_list&quot;&gt;
            &lt;h1&gt;Online Judge 全部题目&lt;/h1&gt;
            &lt;table&gt;
                &lt;tr&gt;
                    &lt;th class=&quot;item&quot;&gt;编号&lt;/th&gt;
                    &lt;th class=&quot;item&quot;&gt;标题&lt;/th&gt;
                    &lt;th class=&quot;item&quot;&gt;难度&lt;/th&gt;
                &lt;/tr&gt;
                {{#questions_list}}
                &lt;tr&gt;
                    &lt;td class=&quot;item&quot;&gt;{{number}}&lt;/td&gt;
                    &lt;!-- 题目跳转链接 --&gt;
                    &lt;td class=&quot;item&quot;&gt;&lt;a href=&quot;/question/{{number}}&quot;&gt;{{title}}&lt;/a&gt;&lt;/td&gt;
                    &lt;td class=&quot;item&quot;&gt;{{difficulty}}&lt;/td&gt;

                &lt;/tr&gt;
                {{/questions_list}}
            &lt;/table&gt;
        &lt;/div&gt;

    &lt;/div&gt;

    &lt;div class=&quot;footer&quot;&gt;
        &lt;hr style=&quot;opacity:0.4;&quot;&gt;
        &lt;p&gt;@yourfriendyo&lt;/p&gt;

    &lt;/div&gt;
&lt;/body&gt;

&lt;/html&gt;
</code></pre>
<h3 id="33-题目编写">3.3 题目编写</h3>
<img src="https://img.gejiba.com/images/ce89f5ae08c922f8e6bba84c2bb5b233.png" style="zoom:50%;" >
<p>网页编写代码的话，我们可以使用ACE在线代码编辑器。</p>
<p>原生js较为复杂，我们使用更简单的JQuery框架，来进行我们的前后端交互。</p>
<pre><code class="language-html">&lt;!-- 题目编写 --&gt;
&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;&gt;

&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge&quot;&gt;
    &lt;title&gt;{{number}}.{{title}}&lt;/title&gt;
    &lt;!-- 引入ACE CDN --&gt;
    &lt;script src=&quot;https://cdnjs.cloudflare.com/ajax/libs/ace/1.2.6/ace.js&quot; type=&quot;text/javascript&quot;
        charset=&quot;utf-8&quot;&gt;&lt;/script&gt;
    &lt;script src=&quot;https://cdnjs.cloudflare.com/ajax/libs/ace/1.2.6/ext-language_tools.js&quot; type=&quot;text/javascript&quot;
        charset=&quot;utf-8&quot;&gt;&lt;/script&gt;
    &lt;!-- 引入jquery CDN --&gt;
    &lt;script src=&quot;http://code.jquery.com/jquery-2.1.1.min.js&quot;&gt;&lt;/script&gt;

    &lt;style&gt;
        * {
            margin: 0;
            padding: 0;
        }

        html,
        body {
            width: 100%;
            height: 100%;
        }

        .container .navbar {
            width: 100%;
            height: 50px;
            background-color: #343333;
            overflow: hidden;
        }

        .container .navbar a {
            display: inline-block;
            width: 40px;
            color: #f5f5f7;
            font-size: 15px;
            font-family: &quot;PingFang SC&quot;;
            line-height: 40px;
            text-decoration: none;
            margin-top: 5px;
            margin-left: 10px;
        }

        .container .navbar a:hover {
            font-weight: bold;
        }

        .container .part1 {
            width: 100%;
            height: 600px;
            overflow: hidden;
        }

        .container .part1 .left_desc {
            width: 50%;
            height: 600px;
            float: left;
            overflow: scroll;
        }

        .container .part1 .left_desc h3 {
            padding-top: 10px;
            padding-left: 10px;
        }

        .container .part1 .left_desc pre {
            padding-top: 10px;
            padding-left: 10px;
            font-size: medium;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        .container .part1 .right_code .ace_text-input {
            width: 50%;
            float: right;
            font-family: 'Courier New', 'Gill Sans', 'Gill Sans MT', 'Trebuchet MS', sans-serif;
        }

        .container .part1 .right_code .ace_editor {
            height: 600px;
        }

        .container .part2 {
            width: 100%;
            overflow: hidden;
        }

        .container .part2 .result {
            width: 300px;
            float: left;
        }

        .container .part2 .btn-submit {
            width: 120px;
            height: 50px;
            font-size: large;
            float: right;
            background-color: #26bb9c;
            color: #FFF;
            border: 0px;
            margin-top: 10px;
            margin-right: 10px;
        }

        .container .part2 button:hover {
            color: green;
        }

        .container .part2 .result {
            margin-top: 15px;
            margin-left: 15px;
            font-size: large;
            font-family: 'Dejavu Sans Mono', 'Courier New';
        }

        .container .part2 .result pre {
            font-size: medium;
        }
    &lt;/style&gt;
&lt;/head&gt;

&lt;body&gt;
    &lt;div class=&quot;container&quot;&gt;
        &lt;!-- 导航栏， 功能不实现--&gt;
        &lt;div class=&quot;navbar&quot;&gt;
            &lt;a href=&quot;/&quot;&gt;首页&lt;/a&gt;
            &lt;a href=&quot;/all_questions&quot;&gt;题库&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;竞赛&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;讨论&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;求职&lt;/a&gt;
            &lt;a href=&quot;#&quot;&gt;登录&lt;/a&gt;
        &lt;/div&gt;

        &lt;!-- 左右呈现，题目描述和预设代码 --&gt;
        &lt;div class=&quot;part1&quot;&gt;
            &lt;div class=&quot;left_desc&quot;&gt;
                &lt;h3&gt;&lt;span id=&quot;number&quot;&gt;{{number}}&lt;/span&gt; . {{title}} _ {{difficulty}}&lt;/h3&gt;
                &lt;pre&gt;{{desc}}&lt;/pre&gt;
            &lt;/div&gt;
            &lt;div class=&quot;right_code&quot;&gt;
                &lt;pre id=&quot;code&quot; class=&quot;ace_editor&quot;&gt;&lt;textarea class=&quot;ace_text-input&quot;&gt;{{header_code}}&lt;/textarea&gt;&lt;/pre&gt;
            &lt;/div&gt;
        &lt;/div&gt;

        &lt;!-- 提交并且得到结果，并显示 --&gt;
        &lt;div class=&quot;part2&quot;&gt;
            &lt;div class=&quot;result&quot;&gt;&lt;/div&gt;
            &lt;button class=&quot;btn-submit&quot; onclick=&quot;submit()&quot;&gt;提交代码&lt;/button&gt;
        &lt;/div&gt;

    &lt;/div&gt;

    &lt;script&gt;
        //初始化对象
        editor = ace.edit(&quot;code&quot;);

        editor.setTheme(&quot;ace/theme/monokai&quot;);
        editor.session.setMode(&quot;ace/mode/c_cpp&quot;);

        // 字体
        editor.setFontSize(15);
        editor.getSession().setTabSize(4);
        editor.setReadOnly(false);

        // 启用提示菜单
        ace.require(&quot;ace/ext/language_tools&quot;);
        editor.setOptions({
            enableBasicAutocompletion: true,
            enableSnippets: true,
            enableLiveAutocompletion: true
        });

        // 提交信息
        function submit() {
            //1. 收集数据，题号和代码
            var code = editor.getSession().getValue();
            var number = $(&quot;.container .part1 .left_desc h3 #number&quot;).text();

            //2. 构建json，通过ajax并向后台发起请求
            var judge_url = &quot;/judge/&quot; + number;
            $.ajax({
                method: 'Post',
                url: judge_url,
                dataType: 'json', // 告知后端需要的数据格式

                contentType: 'application/json; charset=utf-8', // 向后端发送的数据格式
                data: JSON.stringify({
                    'code': code,
                    'input': ''
                }),

                //3. 得到后台结果，显示到前端页面中
                success: function (data) // 交互成功后执行的回调函数
                {
                    show_result(data);
                }
            });
        }

        function show_result(data) {
            var result_div = $(&quot;.container .part2 .result&quot;);

            var reason_div = $(&quot;&lt;p&gt;&quot;, {
                text: data.reason
            });
            reason_div.appendTo(result_div);

            if (data.status == 0) {
                var stdout_lable = $(&quot;&lt;pre&gt;&quot;, {
                    text: data.stdout
                });
                stdout_lable.appendTo(result_div);

                var stderr_lable = $(&quot;&lt;pre&gt;&quot;, {
                    text: data.stderr
                });
                stdout_lable.appendTo(result_div);
            }
            else { } // 编译失败
        }

    &lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<h3 id="34-数据库化">3.4 数据库化</h3>
<p>原本我们的题库采用的是文件的形式存储在项目目录下的：</p>
<pre><code class="language-shell">oj_server
└── questions_all      # 题库中每个题目的配套文件 
    ├── 1
    │   ├── desc.txt
    │   ├── header.cpp
    │   └── tail.cpp
    ├── 2
    │   ├── desc.txt
    │   ├── header.cpp
    │   └── tail.cpp
    └── questions.conf
    │
    └──  # ...
</code></pre>
<blockquote>
<p>现在我们要将其放到数据库中，摆脱对文件的依赖。我会保留一个文件版<code>oj_server_file_version</code>板块。</p>
</blockquote>
<p>首先我们要有一个能够远程登录的MySQL用户，然后我们就可以设计表结构并开始编码。不过目前项目整体难度已经不小且成本不低，所以我们计划用一张表<code>oj_questions</code>就完成数据库化的任务。</p>
<pre><code class="language-mysql">create user oj_client@'%' identified by 'OJoj_123456';
create database oj;
use oj;
grant all on oj.* to oj_client@'%';
</code></pre>
<p>我们将<code>question.conf</code>题库列表文件和<code>questions_all/(+d)</code>单个题目的相关文件设计到一张表中。</p>
<p>执行下面sql语句构建和填充库表。</p>
<pre><code class="language-mysql">use oj;

create table if not exists `oj_questions` (
	`number` int primary key auto_increment comment '题目编号',
	`title` varchar(128) NOT NULL comment '题目标题',
	`difficulty` varchar(8) NOT NULL comment '题目难度',
	`dsec` text NOT NULL comment '题目描述',
	`header` text NOT NULL comment '题目预设代码',
	`tail` text NOT NULL comment '题目测试用例代码',
	`cpu_lim` int default 1 comment '题目的cpu时间',
	`mem_lim` int default 40960 comment '题目的内存限制'
)engine=InnoDB default charset=utf8;

desc oj_questions;

INSERT INTO oj.oj_questions
(`number`, title, difficulty, dsec, header, tail, cpu_lim, mem_lim)
VALUES(1, '两数之和', '简单', '给定一个整数数组`nums`和一个整数目标值 `target`，请你在该数组中找出**和为目标值**`target`的那**两个**整数，并返回它们的数组**下标**。

你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。

你可以按任意顺序返回答案。

示例 1：
```
输入：nums = [2,7,11,15], target = 9
输出：[0,1]
解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。
```

示例 2：
```
输入：nums = [3,2,4], target = 6
输出：[1,2]
```

示例 3：
```
输入：nums = [3,3], target = 6
输出：[0,1]
```

提示：

```
2 &lt;= nums.length &lt;= 104
-109 &lt;= nums[i] &lt;= 109
-109 &lt;= target &lt;= 109
```

只会存在一个有效答案

进阶：你可以想出一个时间复杂度小于 O(n2) 的算法吗？', '#include &lt;iostream&gt;
#include &lt;string&gt;
#include &lt;vector&gt;
#include &lt;map&gt;
#include &lt;algorithm&gt;
using namespace std;

class Solution {
public:
    vector&lt;int&gt; twoSum(vector&lt;int&gt;&amp; nums, int target) {
        // coding here...

    }
};', '// g++ -D COMPILER_SERVER 编译代码时加宏定义即可去掉下面调包含语句
#ifndef COMPILER_SERVER
#include &quot;header.cpp&quot;
#endif

void Test1()
{
    vector&lt;int&gt; nums = { 2,7,11,15 };
    vector&lt;int&gt; ret = Solution().twoSum(nums, 9);
    if (find(ret.begin(), ret.end(), 0) != ret.end() &amp;&amp;
        find(ret.begin(), ret.end(), 1) != ret.end())
    {
        std::cout &lt;&lt; &quot;通过测试用例1&quot; &lt;&lt; std::endl;
    }
    else
    {
        std::cout &lt;&lt; &quot;测试用例1失败&quot; &lt;&lt; std::endl;
    }
}

void Test2()
{
    vector&lt;int&gt; nums = { 3,2,4 };
    vector&lt;int&gt; ret = Solution().twoSum(nums, 6);
    if (find(ret.begin(), ret.end(), 2) != ret.end() &amp;&amp;
        find(ret.begin(), ret.end(), 4) != ret.end())
    {
        std::cout &lt;&lt; &quot;通过测试用例2&quot; &lt;&lt; std::endl;
    }
    else
    {
        std::cout &lt;&lt; &quot;测试用例2失败&quot; &lt;&lt; std::endl;
    }
}

void Test3()
{
    vector&lt;int&gt; nums = { 3,3 };
    vector&lt;int&gt; ret = Solution().twoSum(nums, 6);
    if (find(ret.begin(), ret.end(), 0) != ret.end() &amp;&amp;
        find(ret.begin(), ret.end(), 1) != ret.end())
    {
        std::cout &lt;&lt; &quot;通过测试用例1&quot; &lt;&lt; std::endl;
    }
    else
    {
        std::cout &lt;&lt; &quot;测试用例1失败&quot; &lt;&lt; std::endl;
    }
}

int main()
{
    Test1();
    Test2();
    Test3();

    return 0;
}', 1, 30720);

INSERT INTO oj.oj_questions
(`number`, title, difficulty, dsec, header, tail, cpu_lim, mem_lim)
VALUES(2, '求最大值', '简单', '输入10个整数，要求输出其中的最大值。

输入描述:

测试数据有多组，每组10个整数。

输出描述:

示例1

输入：10 22 23 152 65 79 85 96 32 1

输出：max=152', '#include &lt;iostream&gt;
#include &lt;vector&gt;
#include &lt;algorithm&gt;

using namespace std;

class Solution
{
public:
    int Max(const vector&lt;int&gt;&amp; nums)
    {
        //coding here...

    }
};', '// g++ -D COMPILER_SERVER 编译代码时加宏定义即可去掉下面调包含语句
#ifndef COMPILER_SERVER
#include &quot;header.cpp&quot;
#endif

void Test1()
{
    vector&lt;int&gt; nums = { 10,22,23,152,65,79,85,96,32,1 };
    if (Solution().Max(nums) == 152)
        std::cout &lt;&lt; &quot;测试用例1通过&quot; &lt;&lt; std::endl;
    else
        std::cout &lt;&lt; &quot;测试用例1失败&quot; &lt;&lt; std::endl;
}

void Test2()
{
    vector&lt;int&gt; nums = { 2,34,5,243,351,412,2,8,56,89,138, };
    if (Solution().Max(nums) == 412)
        std::cout &lt;&lt; &quot;测试用例2通过&quot; &lt;&lt; std::endl;
    else
        std::cout &lt;&lt; &quot;测试用例2失败&quot; &lt;&lt; std::endl;
}

int main()
{
    Test1();
    Test2();

    return 0;
}', 1, 40960);
</code></pre>
<h4 id="代码连接mysql">代码连接MySQL</h4>
<h5 id="安装环境">安装环境</h5>
<p>要想使用C/C++的MySQL接口，必须使用MySQL提供的库。如果当初是用yum安装MySQL的直接就有。</p>
<img src="https://img.gejiba.com/images/0e99187e24b6a23ff917f76efcc905fa.png" style="zoom:50%;" >
<p>如果没有目录<code>/usr/include/mysql</code>，可以安装如下两个程序</p>
<pre><code class="language-shell">$ rpm --import https://repo.mysql.com/RPM-GPG-KEY-mysql-2022 # 如果出现GPG key不匹配的问题
$ yum install -y libodb-mysql-devel.x86_64
$ yum install -y soci-mysql-devel.x86_64 
</code></pre>
<p>链接静态库的时候，要指明库名和路径。</p>
<pre><code class="language-makefile">mysql_test:mysql_test.cc
        g++ -o $@ $^ -lmysqlclient -L/usr/lib64/mysql -std=c++11
</code></pre>
<blockquote>
<p>这里有一个坑，库所在路径一定要指明<code>/usr/lib64/mysql</code>，如果找不到的话。</p>
</blockquote>
<p>我们的oj服务是基于MVC模式的，所以我们只需要改动<code>oj_model.hpp</code>即可。接口定义不变，只改变中各实现即可。</p>
<pre><code class="language-cpp">#pragma once
#include &lt;iostream&gt;
#include &lt;string&gt;
#include &lt;vector&gt;
#include &lt;unordered_map&gt;
#include &lt;fstream&gt;
#include &lt;mutex&gt;
#include &lt;cassert&gt;
#include &lt;mysql/mysql.h&gt;
#include &quot;../comm/log.hpp&quot;
#include &quot;../comm/util.hpp&quot;

// oj_model.hpp 提供数据交互访问题目的模块功能
namespace NS_Model
{
    using namespace NS_Log;
    using namespace NS_Util;

    struct Question
    {
        std::string _number;     // 题目编号
        std::string _title;      // 题目标题
        std::string _desc;       // 题目描述
        std::string _difficulty; // 题目难度 简单 中等 困难

        std::string _header;     // 题目的预设代码
        std::string _tail;       // 题目的测试用例

        int _cpu_limit;          // 题目的时间要求
        int _mem_limit;          // 题目的空间要求
    };

    const std::string tb_name = &quot;oj_questions&quot;;
    const char* host = &quot;127.0.0.1&quot;;
    const char* user = &quot;oj_client&quot;;
    const char* passwd = &quot;OJoj123456&quot;;
    const char* db_name = &quot;oj&quot;;
    const int port = 3306;

    //MYSQL* _mysql_conn = nullptr;
    class Model
    {
    private:
        std::mutex mtx;
        MYSQL* _mysql_conn = nullptr;

    public:
        Model()
        {
            if (_mysql_conn == nullptr)
            {
                std::lock_guard&lt;std::mutex&gt; lck(mtx);
                if (_mysql_conn == nullptr)
                {
                    _mysql_conn = mysql_init(nullptr);

                    // 连接数据库
                    if (mysql_real_connect(_mysql_conn,
                        host, user, passwd, db_name, port, nullptr, 0) == nullptr)
                    {
                        LOG(FATAL) &lt;&lt; &quot;connect mysql failed\n&quot;;
                    }
                    LOG(INFO) &lt;&lt; &quot;connect mysql success\n&quot;;

                    mysql_set_character_set(_mysql_conn, &quot;utf8&quot;);
                }
            }
        }

        ~Model()
        {
            mysql_close(_mysql_conn);
        }

        bool QueryMysql(const std::string&amp; sql, std::vector&lt;Question&gt;* out)
        {
            if (mysql_query(_mysql_conn, sql.c_str()) != 0)
            {
                LOG(WARNING) &lt;&lt; sql &lt;&lt; &quot; execute error\n&quot;;
                return false;
            }

            // 提取结果
            MYSQL_RES* res = mysql_store_result(_mysql_conn);

            int rows = mysql_num_rows(res);   // 行数
            for (int i = 0; i &lt; rows; i++)
            {
                MYSQL_ROW row = mysql_fetch_row(res);

                Question q;
                q._number = row[0];
                q._title = row[1];
                q._difficulty = row[2];
                q._desc = row[3];
                q._header = row[4];
                q._tail = row[5];
                q._cpu_limit = atoi(row[6]);
                q._mem_limit = atoi(row[7]);

                out-&gt;push_back(q);
            }

            mysql_free_result(res);
            return true;
        }

        bool GetAllQuestions(std::vector&lt;Question&gt;* out)
        {
            std::string sql = &quot;select * from &quot; + tb_name;
            return QueryMysql(sql, out);
        }

        bool GetOneQuestion(const std::string&amp; number, Question* question)
        {
            std::vector&lt;Question&gt; res;
            std::string sql = &quot;select * from &quot; + tb_name += &quot; where number=&quot; + number;
            if (QueryMysql(sql, &amp;res))
            {
                if (res.size() == 1)
                    *question = res[0];
                else
                    return false;
            }
            return true;
        }
    };
}
</code></pre>
<p> </p>
<h2 id="4-项目总结和项目扩展">4. 项目总结和项目扩展</h2>
<h3 id="项目亮点">项目亮点</h3>
<ul>
<li>
<p>使用多个知名的开源库，如<code>Boost</code>、<code>cpp-httplib</code>、<code>ctemplate</code>、<code>jsoncpp</code>。</p>
</li>
<li>
<p>良好的负载均衡方案设计。</p>
</li>
<li>
<p>支持多进程和多线程。</p>
</li>
<li>
<p>有一定的前端技术美化项目。</p>
</li>
</ul>
<h3 id="项目扩展">项目扩展</h3>
<ol>
<li>附带注册登录的录题功能；</li>
<li>接入社区功能或者博客系统；</li>
<li>把编译服务部署在docker上；</li>
<li>将编译服务包装成远程过程调用服务<code>rest_rpc</code>替换http服务。</li>
<li>丰富做题界面的功能，如下一题按钮。</li>
</ol>
]]></content>
    </entry>
</feed>